# Ping v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Spec 문서 `docs/superpowers/specs/2026-05-20-recapture-replay-screen-history-design.md`를 따라 Ping v0.2를 출시한다 — 다시 찍기/재생 UX, 전체화면+얼굴 PIP 캡쳐, 룸 히스토리 피드, 메뉴바 정리 & 응답성 개선.

**Architecture:** Spec별로 Phase 분리. 각 Phase는 독립적으로 ship 가능. Phase 내부는 TDD 단위(작성→실패→구현→통과→commit)로 분해. 데이터 모델/마이그레이션 → service layer → UI 순서로 진행해 의존성 역행 방지.

**Tech Stack:** Swift 6 + SwiftUI/AppKit, AVFoundation, ScreenCaptureKit (macOS 13+), Supabase Postgres RPC + Storage + RLS, KeyboardShortcuts 패키지, XCTest.

**Phase 순서:**
1. **Phase A0** — 녹화 시간 3초 + audio warm-up bug fix (v0.2.1 amendment, prerequisite)
2. **Phase A** — 다시 찍기 + 다시 재생 (Spec A) — foundation
3. **Phase D1** — 메뉴바 파트너 표시 제거 (Spec D1) — 작고 독립
4. **Phase B0** — 캡쳐 모드 단축키 분리 (v0.2.1 amendment, Phase B prerequisite)
5. **Phase B** — 전체화면+얼굴 PIP 캡쳐 (Spec B) — Phase A의 reviewing state 활용
6. **Phase C** — 룸 히스토리 피드 (Spec C) — Phase B의 데이터 모델 활용
7. **Phase D2** — 메뉴바 응답성 개선 (Spec D2) — 측정 기반 fix

각 Phase 끝에서 `xcodebuild test`가 통과해야 다음 Phase로 진행한다.

> **v0.2.1 amendment 적용 사항** (2026-05-21 결정, spec 문서의 동명 amendment 섹션 참조):
> - 녹화 시간 2초 → **3초** (모든 task에서 2초 표기는 3초로 해석)
> - audio warm-up 버그 fix 추가 (Phase A0)
> - 캡쳐 모드는 두 글로벌 단축키로 분리 (Option+P / Option+L), F 토글 제거 (Phase B0)
> - `UserPreferences.lastCaptureMode` 저장 및 Settings 캡쳐 모드 라디오 제거 → 영향 task: Task B3, B8, B13에 적용

---

## Phase A0 — 녹화 시간 3초 + audio warm-up fix (v0.2.1 prerequisite)

### Task A0.1: Audio configuration을 camera start와 함께 — 1초 버그 fix

**Files:**
- Modify: `Ping/Capture/CameraManager.swift`
- Modify: `Ping/UI/Mirror/MirrorView.swift`

**1초 버그 원인:** `CameraManager.prepareAudioForRecording`이 첫 녹화 시점에 `AVCaptureSession.beginConfiguration / commitConfiguration` 동기 수행 → session reconfigure 동안 첫 frame 도착이 약 1초 지연 → `maxRecordedDuration`은 startRecording부터 카운트되므로 영상은 절반만 캡쳐.

**해결:** audio를 camera 첫 start와 함께 미리 configure. `prepareAudioForRecording`은 idempotent 유지 (이미 `audioConfigured` flag 있음). 추가로 `startRecording` 직전 짧은 wait.

- [ ] **Step 1: CameraManager에 startWithAudio 메서드 추가**

In `Ping/Capture/CameraManager.swift`, add:

```swift
    func startWithAudio() async {
        await start()
        guard isReady else { return }
        await prepareAudioForRecording()
    }
```

- [ ] **Step 2: AppDelegate의 카메라 시작 지점에서 audio 함께 configure**

Find where `camera.start()` is called for mirror display (likely in `showMirror()` or after).

Replace `await camera.start()` with `await camera.startWithAudio()` in those entry points.

- [ ] **Step 3: MirrorView.startRecording에서 prepareAudioForRecording 후 짧은 wait**

In `Ping/UI/Mirror/MirrorView.swift`, in `startRecording()`, replace:

```swift
        await camera.prepareAudioForRecording()
        let recorder = VideoRecorder(output: camera.movieOutput)
```

with:

```swift
        await camera.prepareAudioForRecording()
        // Audio configuration may briefly destabilize session; wait for first frame to land.
        try? await Task.sleep(for: .milliseconds(150))
        let recorder = VideoRecorder(output: camera.movieOutput)
```

(`prepareAudioForRecording`이 idempotent라 정상 case에선 즉시 return, 150ms는 launch warm-up 경로에만 영향.)

- [ ] **Step 4: Build and test**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`
Expected: All existing tests pass.

- [ ] **Step 5: Commit**

```bash
git add Ping/Capture/CameraManager.swift Ping/UI/Mirror/MirrorView.swift
git commit -m "fix(capture): warm up audio with camera to avoid first-frame delay"
```

---

### Task A0.2: 녹화 시간 2초 → 3초

**Files:**
- Modify: `Ping/Capture/VideoRecorder.swift`
- Modify: `Ping/UI/Mirror/MirrorViewModel.swift`
- Modify: `Ping/UI/Mirror/MirrorView.swift`
- Create: `supabase/migrations/20260521000050_v0_2_duration_3s.sql`

- [ ] **Step 1: VideoRecorder maxRecordedDuration 3초**

In `Ping/Capture/VideoRecorder.swift`, change line 21:

```swift
movieOutput.maxRecordedDuration = CMTime(seconds: 3.0, preferredTimescale: 600)
```

Also rename function for clarity (the existing `recordTwoSeconds` is misleading at 3 seconds):

```swift
    func recordClip(seconds: Double = 3.0) async throws -> URL {
        let tempURL = FileManager.default.temporaryDirectory
            .appendingPathComponent("ping-\(UUID().uuidString).mp4")

        if movieOutput.isRecording {
            movieOutput.stopRecording()
        }

        movieOutput.maxRecordedDuration = CMTime(seconds: seconds, preferredTimescale: 600)
        movieOutput.startRecording(to: tempURL, recordingDelegate: self)
        // ... rest unchanged
    }
```

(Replace the old `recordTwoSeconds()` definition with `recordClip(seconds:)`. Find callers and update.)

- [ ] **Step 2: MirrorViewModel.countdown default 3**

In `Ping/UI/Mirror/MirrorViewModel.swift`:

```swift
    @Published var countdown: Int = 3

    func redo() {
        state = .recording
        countdown = 3
    }

    func reset() {
        state = .idle
        countdown = 3
    }
```

- [ ] **Step 3: MirrorView countdown 시퀀스 3 → 2 → 1**

In `Ping/UI/Mirror/MirrorView.swift`, in `startRecording()`:

```swift
        viewModel.state = .recording
        viewModel.countdown = 3

        await camera.prepareAudioForRecording()
        try? await Task.sleep(for: .milliseconds(150))
        let recorder = VideoRecorder(output: camera.movieOutput)
        let countdownTask = Task { @MainActor in
            try? await Task.sleep(for: .seconds(1))
            viewModel.countdown = 2
            try? await Task.sleep(for: .seconds(1))
            viewModel.countdown = 1
        }

        do {
            let tempURL = try await recorder.recordClip()
            countdownTask.cancel()
            ...
        }
```

Update all `recordTwoSeconds()` callers to `recordClip()`.

- [ ] **Step 4: Update existing PingTests references**

Search and replace `recordTwoSeconds` → `recordClip` in PingTests:

Run: `grep -rn "recordTwoSeconds" Ping PingTests`
Update any caller.

- [ ] **Step 5: DB migration for duration_ms default**

Create `supabase/migrations/20260521000050_v0_2_duration_3s.sql`:

```sql
-- Phase A0.2: clip duration 2s → 3s
alter table public.messages alter column duration_ms set default 3000;

drop function if exists public.ping_create_message(uuid, uuid, text, text, text, double precision, double precision);

create or replace function public.ping_create_message(
    room_uuid uuid,
    receiver_uid uuid,
    sender_nickname_text text,
    video_id_text text,
    video_url_text text,
    x_ratio double precision,
    y_ratio double precision
) returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
    sender uuid := auth.uid();
    new_id uuid;
begin
    if sender is null then
        raise exception 'auth required';
    end if;

    insert into public.messages(
        room_id, sender_uid, receiver_uid, sender_nickname,
        video_id, video_url, duration_ms,
        mirror_position, status
    ) values (
        room_uuid, sender, receiver_uid, sender_nickname_text,
        video_id_text, video_url_text, 3000,
        jsonb_build_object('xRatio', x_ratio, 'yRatio', y_ratio),
        'uploaded'
    ) returning id into new_id;

    return new_id;
end;
$$;

grant execute on function public.ping_create_message(uuid, uuid, text, text, text, double precision, double precision) to authenticated;
```

- [ ] **Step 6: Apply migration**

Run: `npx supabase db push`
Expected: migration applied.

- [ ] **Step 7: Build and test**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`
Expected: all tests pass.

- [ ] **Step 8: Manual smoke**

Run Ping → Option+P → Enter → 카운트다운이 3 → 2 → 1로 표시 → 3초 녹화 후 reviewing state 진입. 녹화된 파일 길이가 3초인지 확인 (Finder의 파일 정보 또는 `ffprobe`).

- [ ] **Step 9: Commit**

```bash
git add Ping/Capture/VideoRecorder.swift Ping/UI/Mirror/MirrorViewModel.swift Ping/UI/Mirror/MirrorView.swift supabase/migrations/20260521000050_v0_2_duration_3s.sql
git commit -m "feat(capture): increase clip duration to 3 seconds"
```

---

## Phase A — 다시 찍기 + 다시 재생

### Task A1: MirrorViewModel에 reviewing 상태 추가 (TDD)

**Files:**
- Modify: `Ping/UI/Mirror/MirrorViewModel.swift`
- Test: `PingTests/MirrorViewModelStateTests.swift` (create)

- [ ] **Step 1: Write failing test**

Create `PingTests/MirrorViewModelStateTests.swift`:

```swift
import XCTest
@testable import Ping

@MainActor
final class MirrorViewModelStateTests: XCTestCase {
    func test_initialState_isIdle() {
        let vm = MirrorViewModel()
        XCTAssertEqual(vm.state, .idle)
        XCTAssertEqual(vm.countdown, 3)
    }

    func test_enterReviewing_setsReviewingStateWithURL() {
        let vm = MirrorViewModel()
        let url = URL(fileURLWithPath: "/tmp/test.mp4")
        vm.enterReviewing(url: url)
        XCTAssertEqual(vm.state, .reviewing(url))
    }

    func test_beginUpload_transitionsFromReviewingToUploading() {
        let vm = MirrorViewModel()
        let url = URL(fileURLWithPath: "/tmp/test.mp4")
        vm.enterReviewing(url: url)
        vm.beginUpload()
        XCTAssertEqual(vm.state, .uploading)
    }

    func test_redo_resetsToRecordingWithCountdown() {
        let vm = MirrorViewModel()
        let url = URL(fileURLWithPath: "/tmp/test.mp4")
        vm.enterReviewing(url: url)
        vm.countdown = 1
        vm.redo()
        XCTAssertEqual(vm.state, .recording)
        XCTAssertEqual(vm.countdown, 3)
    }

    func test_reset_returnsToIdle() {
        let vm = MirrorViewModel()
        vm.state = .failed("err")
        vm.reset()
        XCTAssertEqual(vm.state, .idle)
        XCTAssertEqual(vm.countdown, 3)
    }
}
```

- [ ] **Step 2: Run test to verify it fails (compile error)**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/MirrorViewModelStateTests`
Expected: FAIL — `.reviewing` case not found, `enterReviewing`/`beginUpload`/`redo` not defined.

- [ ] **Step 3: Update MirrorViewModel**

Replace `Ping/UI/Mirror/MirrorViewModel.swift` entirely:

```swift
import Combine
import Foundation

@MainActor
final class MirrorViewModel: ObservableObject {
    enum State: Equatable {
        case idle
        case recording
        case reviewing(URL)
        case uploading
        case failed(String)
    }

    @Published var state: State = .idle
    @Published var countdown: Int = 2

    func enterReviewing(url: URL) {
        state = .reviewing(url)
    }

    func beginUpload() {
        state = .uploading
    }

    func redo() {
        state = .recording
        countdown = 2
    }

    func reset() {
        state = .idle
        countdown = 2
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/MirrorViewModelStateTests`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add Ping/UI/Mirror/MirrorViewModel.swift PingTests/MirrorViewModelStateTests.swift
git commit -m "feat(mirror): add reviewing state to MirrorViewModel"
```

---

### Task A2: MirrorView reviewing UI (preview loop + hint + key handling)

**Files:**
- Modify: `Ping/UI/Mirror/MirrorView.swift`

- [ ] **Step 1: Update `installKeyMonitor` to handle Backspace and reviewing-state Enter**

Replace `installKeyMonitor()` in `Ping/UI/Mirror/MirrorView.swift`:

```swift
    private func installKeyMonitor() {
        keyMonitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { event in
            guard event.window is MirrorWindow else {
                return event
            }

            switch event.keyCode {
            case 36: // Return
                handleReturnKey()
                return nil
            case 51: // Delete/Backspace
                handleBackspaceKey()
                return nil
            case 53: // Escape
                onClose()
                return nil
            case 48: // Tab
                if let next = appState.cycleToNextPartner(currentRoomId: selectedRoomId) {
                    selectedRoomId = next.id
                    appState.sendMode = .singlePartner
                }
                return nil
            case 18, 19, 20, 21, 23, 22, 26, 28, 25:
                if let index = Self.numericKeyIndex(for: event.keyCode),
                   let room = appState.selectPartner(at: index) {
                    selectedRoomId = room.id
                    appState.sendMode = .singlePartner
                }
                return nil
            case 29, 0:
                appState.sendMode = .allPartners
                return nil
            default:
                return event
            }
        }
    }

    private func handleReturnKey() {
        switch viewModel.state {
        case .idle, .failed:
            Task { await startRecording() }
        case .reviewing(let url):
            Task { await uploadReviewedClip(url: url) }
        case .recording, .uploading:
            break
        }
    }

    private func handleBackspaceKey() {
        if case .reviewing(let url) = viewModel.state {
            try? FileManager.default.removeItem(at: url)
            Task { await startRecording() }
        }
    }
```

- [ ] **Step 2: Replace `startRecording` to enter reviewing instead of auto-uploading**

Replace `startRecording()` in `Ping/UI/Mirror/MirrorView.swift`:

```swift
    private func startRecording() async {
        switch viewModel.state {
        case .idle, .reviewing:
            break
        case .failed:
            viewModel.reset()
        case .recording, .uploading:
            return
        }

        if !camera.isReady {
            await camera.start()
            if !camera.isReady {
                viewModel.state = .failed(camera.lastError ?? "카메라 준비 중")
                return
            }
        }

        guard !targetsResolvingFailed() else { return }

        viewModel.state = .recording
        viewModel.countdown = 2

        await camera.prepareAudioForRecording()
        let recorder = VideoRecorder(output: camera.movieOutput)
        let countdownTask = Task { @MainActor in
            try? await Task.sleep(for: .seconds(1))
            viewModel.countdown = 1
        }

        do {
            let tempURL = try await recorder.recordTwoSeconds()
            countdownTask.cancel()
            viewModel.enterReviewing(url: tempURL)
        } catch {
            countdownTask.cancel()
            viewModel.state = .failed(error.localizedDescription)
        }
    }

    @discardableResult
    private func targetsResolvingFailed() -> Bool {
        switch appState.sendMode {
        case .singlePartner:
            if currentRoom() == nil {
                viewModel.state = .failed("파트너 없음")
                return true
            }
        case .allPartners:
            let targets = appState.rooms.filter { $0.memberUids.count >= RoomLimits.minSendableMembers }
            if targets.isEmpty {
                viewModel.state = .failed("파트너 없음")
                return true
            }
        }
        return false
    }

    private func currentTargets() -> [Room] {
        switch appState.sendMode {
        case .singlePartner:
            return currentRoom().map { [$0] } ?? []
        case .allPartners:
            return appState.rooms.filter { $0.memberUids.count >= RoomLimits.minSendableMembers }
        }
    }

    private func uploadReviewedClip(url: URL) async {
        viewModel.beginUpload()

        let origin = windowOrigin()
        let screen = NSScreen.main?.frame ?? .zero
        let center = NSPoint(x: origin.x + 100, y: origin.y + 100)
        let position = ScreenCoordinates.normalize(point: center, in: screen)

        do {
            try await onSend(url, position, currentTargets())
            try? await Task.sleep(for: .milliseconds(300))
            onClose()
            viewModel.reset()
            appState.sendMode = .singlePartner
        } catch {
            viewModel.state = .failed(error.localizedDescription)
        }
    }
```

- [ ] **Step 3: Add reviewing preview view + hint capsule to body**

Add `ReviewLoopPlayerView` struct in same file, and update `mirrorContent` and `topOverlay`/`bottomOverlay` switches to handle `.reviewing`:

Append at bottom of `Ping/UI/Mirror/MirrorView.swift` (after closing brace of `MirrorView`):

```swift
import AVKit

struct ReviewLoopPlayerView: NSViewRepresentable {
    let url: URL

    func makeNSView(context: Context) -> PlayerNSView {
        let view = PlayerNSView()
        view.configure(url: url)
        return view
    }

    func updateNSView(_ nsView: PlayerNSView, context: Context) {}

    final class PlayerNSView: NSView {
        private var player: AVPlayer?
        private var playerLayer: AVPlayerLayer?
        nonisolated(unsafe) private var observer: NSObjectProtocol?

        override init(frame: NSRect) {
            super.init(frame: frame)
            setup()
        }

        required init?(coder: NSCoder) {
            super.init(coder: coder)
            setup()
        }

        func configure(url: URL) {
            let item = AVPlayerItem(url: url)
            let player = AVPlayer(playerItem: item)
            player.isMuted = true
            self.player = player

            let layer = AVPlayerLayer(player: player)
            layer.videoGravity = .resizeAspectFill
            layer.frame = bounds
            self.layer?.addSublayer(layer)
            self.playerLayer = layer

            observer = NotificationCenter.default.addObserver(
                forName: .AVPlayerItemDidPlayToEndTime,
                object: item,
                queue: .main
            ) { [weak player] _ in
                player?.seek(to: .zero)
                player?.play()
            }

            player.play()
        }

        override func layout() {
            super.layout()
            playerLayer?.frame = bounds
        }

        private func setup() {
            wantsLayer = true
            layer = CALayer()
        }

        deinit {
            if let observer {
                NotificationCenter.default.removeObserver(observer)
            }
        }
    }
}
```

- [ ] **Step 4: Update `mirrorContent` to swap camera preview with review loop when reviewing**

Replace `mirrorContent` in `Ping/UI/Mirror/MirrorView.swift`:

```swift
    private var mirrorContent: some View {
        ZStack {
            previewLayer
                .clipShape(Circle())

            if showsRainbowBorder {
                RainbowBorder(lineWidth: 2)
            } else {
                Circle().strokeBorder(borderColor, lineWidth: borderWidth)
            }

            VStack {
                topOverlay
                Spacer()
                bottomOverlay
            }
        }
        .frame(width: 200, height: 200)
    }

    @ViewBuilder private var previewLayer: some View {
        if case .reviewing(let url) = viewModel.state {
            ReviewLoopPlayerView(url: url)
        } else {
            CameraPreviewView(session: camera.session)
        }
    }
```

- [ ] **Step 5: Add reviewing case to `topOverlay` (hint capsule with auto fade-out)**

Replace `topOverlay` in `Ping/UI/Mirror/MirrorView.swift`:

```swift
    @ViewBuilder private var topOverlay: some View {
        switch viewModel.state {
        case .recording:
            HStack {
                Spacer()
                Text("\(viewModel.countdown)")
                    .font(PingFont.numeric)
                    .foregroundStyle(.white)
                    .shadow(radius: 4)
                    .padding(8)
            }
        case .reviewing:
            HintCapsuleView(text: "↵ 보내기 · ⌫ 다시")
                .padding(.top, 14)
        case .failed(let message):
            Text(message)
                .font(PingFont.caption)
                .foregroundStyle(Color.yellow)
                .lineLimit(2)
                .minimumScaleFactor(0.7)
                .multilineTextAlignment(.center)
                .padding(.horizontal, 16)
                .padding(.top, 18)
        default:
            if !camera.isReady {
                Text(camera.lastError ?? "카메라 준비 중")
                    .font(PingFont.caption)
                    .foregroundStyle(.white)
                    .lineLimit(2)
                    .minimumScaleFactor(0.7)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal, 18)
                    .padding(.top, 18)
            }
        }
    }
```

Add `HintCapsuleView` at end of file (after `ReviewLoopPlayerView`):

```swift
struct HintCapsuleView: View {
    let text: String
    @State private var opacity: Double = 1.0

    var body: some View {
        Text(text)
            .font(PingFont.caption)
            .foregroundStyle(.white)
            .padding(.horizontal, 10)
            .padding(.vertical, 5)
            .background {
                Capsule()
                    .fill(PingDesign.Surface.rowFill.opacity(0.84))
                    .pingGlassEffect()
                    .overlay {
                        Capsule()
                            .strokeBorder(PingDesign.Surface.strongHairline.opacity(0.48), lineWidth: 0.8)
                    }
            }
            .opacity(opacity)
            .onAppear {
                opacity = 1.0
                withAnimation(.easeOut(duration: 0.5).delay(2.0)) {
                    opacity = 0
                }
            }
    }
}
```

- [ ] **Step 6: Build, run manual smoke test**

Run:
```bash
xcodegen generate
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
DERIVED=$(xcodebuild -project Ping.xcodeproj -scheme Ping -showBuildSettings | grep -m1 BUILT_PRODUCTS_DIR | awk '{print $3}')
open "$DERIVED/Ping.app"
```

Then manually: Option+P → Enter → 2초 녹화 → 거울 안에 클립이 mute loop → Backspace로 다시 녹화 → Enter로 발사. 거울 안 우상단에 "↵ 보내기 · ⌫ 다시" hint capsule이 2초 후 fade-out.

- [ ] **Step 7: Commit**

```bash
git add Ping/UI/Mirror/MirrorView.swift
git commit -m "feat(mirror): add reviewing state UI with preview loop and key handling"
```

---

### Task A3: PlaybackWindow paused-on-last-frame + 10s timeout + Enter replay (TDD)

**Files:**
- Modify: `Ping/UI/Playback/PlaybackView.swift`
- Modify: `Ping/UI/Playback/PlaybackWindow.swift`
- Test: `PingTests/PlaybackReplayTests.swift` (create)

- [ ] **Step 1: Refactor PlaybackView to expose replay controls**

Replace `Ping/UI/Playback/PlaybackView.swift` entirely:

```swift
import SwiftUI
import AVKit

struct PlaybackView: NSViewRepresentable {
    let url: URL
    let onFirstPlayEnd: @MainActor @Sendable () -> Void
    let controllerBox: ControllerBox

    final class ControllerBox {
        var player: AVPlayer?
        func replay() {
            player?.seek(to: .zero)
            player?.play()
        }
    }

    func makeNSView(context: Context) -> PlayerNSView {
        let view = PlayerNSView()
        view.configure(url: url, onFirstEnd: onFirstPlayEnd, controllerBox: controllerBox)
        return view
    }

    func updateNSView(_ nsView: PlayerNSView, context: Context) {}

    final class PlayerNSView: NSView {
        private var player: AVPlayer?
        private var playerLayer: AVPlayerLayer?
        nonisolated(unsafe) private var observer: NSObjectProtocol?
        nonisolated(unsafe) private var didFirePlayEnd = false

        override init(frame: NSRect) {
            super.init(frame: frame)
            setup()
        }

        required init?(coder: NSCoder) {
            super.init(coder: coder)
            setup()
        }

        func configure(
            url: URL,
            onFirstEnd: @escaping @MainActor @Sendable () -> Void,
            controllerBox: ControllerBox
        ) {
            let item = AVPlayerItem(url: url)
            let player = AVPlayer(playerItem: item)
            self.player = player
            controllerBox.player = player

            let layer = AVPlayerLayer(player: player)
            layer.videoGravity = .resizeAspectFill
            layer.frame = bounds
            self.layer?.addSublayer(layer)
            self.playerLayer = layer

            observer = NotificationCenter.default.addObserver(
                forName: .AVPlayerItemDidPlayToEndTime,
                object: item,
                queue: .main
            ) { [weak self, weak player] _ in
                player?.pause()
                guard let self, !self.didFirePlayEnd else { return }
                self.didFirePlayEnd = true
                Task { @MainActor in onFirstEnd() }
            }

            player.play()
        }

        override func layout() {
            super.layout()
            playerLayer?.frame = bounds
        }

        private func setup() {
            wantsLayer = true
            layer = CALayer()
        }

        deinit {
            if let observer {
                NotificationCenter.default.removeObserver(observer)
            }
        }
    }
}
```

- [ ] **Step 2: Update PlaybackWindow with paused state + key monitor + 10s timeout**

Replace `Ping/UI/Playback/PlaybackWindow.swift` entirely:

```swift
import AppKit
import SwiftUI

@MainActor
final class PlaybackWindow: NSWindow {
    static let size = NSSize(width: 200, height: 200)
    static let pausedTimeoutSeconds: Double = 10
    var pingWindowId = UUID()

    private let controllerBox = PlaybackView.ControllerBox()
    private var keyMonitor: Any?
    private var timeoutTask: Task<Void, Never>?
    private let onDone: @MainActor @Sendable () -> Void
    private let onFirstPlayEnd: @MainActor @Sendable () -> Void

    init(
        videoURL: URL,
        atScreenPoint origin: NSPoint,
        onFirstPlayEnd: @escaping @MainActor @Sendable () -> Void,
        onDone: @escaping @MainActor @Sendable () -> Void
    ) {
        self.onDone = onDone
        self.onFirstPlayEnd = onFirstPlayEnd

        super.init(
            contentRect: NSRect(origin: origin, size: Self.size),
            styleMask: [.borderless],
            backing: .buffered,
            defer: false
        )
        isOpaque = false
        backgroundColor = .clear
        level = .floating
        hasShadow = false
        ignoresMouseEvents = true
        collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        isReleasedWhenClosed = false

        let root = ZStack {
            PlaybackView(url: videoURL, onFirstPlayEnd: { [weak self] in
                self?.handleFirstPlayEnd()
            }, controllerBox: controllerBox)
            .clipShape(Circle())

            Circle().strokeBorder(Color.white.opacity(0.30), lineWidth: 1)
        }
        .frame(width: Self.size.width, height: Self.size.height)

        let host = NSHostingView(rootView: AnyView(root))
        host.frame = NSRect(origin: .zero, size: Self.size)
        contentView = host
        alphaValue = 0
    }

    func fadeIn() {
        makeKeyAndOrderFront(nil)
        installKeyMonitor()
        NSAnimationContext.runAnimationGroup { context in
            context.duration = 0.20
            animator().alphaValue = 1.0
        }
    }

    private func installKeyMonitor() {
        if keyMonitor != nil { return }
        keyMonitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { [weak self] event in
            guard let self, event.window === self else { return event }
            switch event.keyCode {
            case 36: // Return
                self.handleReplay()
                return nil
            case 53: // Escape
                self.handleClose()
                return nil
            default:
                return event
            }
        }
    }

    private func handleFirstPlayEnd() {
        onFirstPlayEnd()
        startTimeoutCountdown()
    }

    private func handleReplay() {
        timeoutTask?.cancel()
        controllerBox.replay()
        // Will pause at end again; AVPlayer notification fires once per item,
        // so we need to re-observe. Easiest: poll via timer until pause.
        // But our PlayerNSView's didFirePlayEnd flag is sticky — fix:
        // We add a secondary observer here.
        startReplayEndObserver()
    }

    private var replayObserver: NSObjectProtocol?

    private func startReplayEndObserver() {
        if let replayObserver {
            NotificationCenter.default.removeObserver(replayObserver)
        }
        guard let item = controllerBox.player?.currentItem else { return }
        replayObserver = NotificationCenter.default.addObserver(
            forName: .AVPlayerItemDidPlayToEndTime,
            object: item,
            queue: .main
        ) { [weak self] _ in
            Task { @MainActor in
                self?.controllerBox.player?.pause()
                self?.startTimeoutCountdown()
            }
        }
    }

    private func startTimeoutCountdown() {
        timeoutTask?.cancel()
        timeoutTask = Task { @MainActor in
            try? await Task.sleep(for: .seconds(Self.pausedTimeoutSeconds))
            guard !Task.isCancelled else { return }
            handleClose()
        }
    }

    private func handleClose() {
        timeoutTask?.cancel()
        if let keyMonitor {
            NSEvent.removeMonitor(keyMonitor)
            self.keyMonitor = nil
        }
        if let replayObserver {
            NotificationCenter.default.removeObserver(replayObserver)
            self.replayObserver = nil
        }
        fadeOutAndClose()
    }

    private func fadeOutAndClose() {
        NSAnimationContext.runAnimationGroup({ context in
            context.duration = 0.30
            animator().alphaValue = 0
        }, completionHandler: { [weak self] in
            Task { @MainActor in
                self?.orderOut(nil)
                self?.onDone()
            }
        })
    }
}
```

- [ ] **Step 3: Update AppDelegate `playMessage` to use new init**

Replace `playMessage` in `Ping/AppDelegate.swift` (around line 330):

```swift
    private func playMessage(messageId: String) {
        Task { @MainActor in
            do {
                guard let message = try await messageService.get(messageId: messageId) else { return }
                let localURL = try await cachedVideoURL(for: message)

                let screen = NSScreen.main?.visibleFrame ?? .zero
                let center = ScreenCoordinates.denormalize(position: message.mirrorPosition, in: screen)
                let origin = ScreenCoordinates.clamp(
                    point: NSPoint(x: center.x - PlaybackWindow.size.width / 2,
                                   y: center.y - PlaybackWindow.size.height / 2),
                    windowSize: PlaybackWindow.size,
                    inSafeArea: screen
                )

                let windowId = UUID()
                let window = PlaybackWindow(
                    videoURL: localURL,
                    atScreenPoint: origin,
                    onFirstPlayEnd: { [weak self] in
                        Task { @MainActor in
                            try? await self?.messageService.markSeen(messageId: messageId)
                        }
                    },
                    onDone: { [weak self] in
                        Task { @MainActor in
                            if !LocalArchive.saveReceivedEnabled {
                                try? FileManager.default.removeItem(at: localURL)
                                self?.playbackCache[messageId] = nil
                            }
                            self?.playbackWindows.removeAll { $0.pingWindowId == windowId }
                        }
                    }
                )
                window.pingWindowId = windowId
                playbackWindows.append(window)
                window.fadeIn()
            } catch {
                NSLog("Playback failed: \(error)")
            }
        }
    }
```

- [ ] **Step 4: Build and run manual smoke test for playback**

Run:
```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

Then manually: 다른 Mac/계정에서 ping 전송 → 알림 클릭 → 자동 재생 → 마지막 프레임 정지 → 10초 안에 Enter 누르면 재재생, Esc 누르면 즉시 닫힘, 아무것도 안 누르면 10초 후 자동 닫힘.

- [ ] **Step 5: Commit**

```bash
git add Ping/UI/Playback/PlaybackView.swift Ping/UI/Playback/PlaybackWindow.swift Ping/AppDelegate.swift
git commit -m "feat(playback): paused-on-last-frame with Enter replay and 10s timeout"
```

---

### Task A4: Phase A 회귀 테스트 & 마무리

- [ ] **Step 1: Full test pass**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`
Expected: All tests pass including new `MirrorViewModelStateTests`.

- [ ] **Step 2: End-to-end smoke test (manual)**

녹화→ reviewing→ Backspace→ 재녹화→ Enter→ 업로드→ 수신측 자동재생→ paused→ Enter→ 재재생→ Esc→ 닫힘. 전 flow 한 cycle 무결성 확인.

- [ ] **Step 3: Phase A 종료 commit**

```bash
git commit --allow-empty -m "chore(phase-a): 다시 찍기 + 다시 재생 완료"
```

---

## Phase D1 — 메뉴바 파트너 표시 제거

(Phase D2의 응답성 개선과 분리해서 작은 작업 먼저)

### Task D1: 파트너 메뉴 아이템 제거

**Files:**
- Modify: `Ping/StatusMenuBuilder.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Remove partner item from StatusMenuBuilder**

Replace `Ping/StatusMenuBuilder.swift` entirely:

```swift
import AppKit
import KeyboardShortcuts

@MainActor
enum StatusMenuBuilder {
    static func makeMenu(target: AnyObject) -> NSMenu {
        let menu = NSMenu()

        let status = NSMenuItem(title: "Ping", action: nil, keyEquivalent: "")
        status.isEnabled = false
        menu.addItem(status)
        menu.addItem(NSMenuItem.separator())

        menu.addItem(command(title: "영상 보내기", action: Selector(("toggleMirrorAction")), shortcutName: .pingTrigger, target: target))
        menu.addItem(command(title: "내 룸…", action: Selector(("showRoomManager")), target: target))
        menu.addItem(command(title: "설정…", action: Selector(("showSettings")), keyEquivalent: ",", target: target))
        menu.addItem(command(title: "라이트/다크 전환", action: Selector(("toggleAppearanceModeAction")), shortcutName: .appearanceToggle, target: target))

        menu.addItem(NSMenuItem.separator())
        let updateItem = NSMenuItem(
            title: "업데이트 확인…",
            action: #selector(UpdaterController.checkForUpdates(_:)),
            keyEquivalent: ""
        )
        updateItem.target = UpdaterController.shared
        menu.addItem(updateItem)

        menu.addItem(NSMenuItem.separator())
        menu.addItem(NSMenuItem(title: "종료", action: #selector(NSApplication.terminate(_:)), keyEquivalent: "q"))

        return menu
    }

    private static func command(
        title: String,
        action: Selector,
        keyEquivalent: String = "",
        shortcutName: KeyboardShortcuts.Name? = nil,
        target: AnyObject
    ) -> NSMenuItem {
        let item = NSMenuItem(title: title, action: action, keyEquivalent: keyEquivalent)
        if let shortcutName {
            applyShortcut(shortcutName, to: item)
        }
        item.target = target
        return item
    }

    private static func applyShortcut(_ name: KeyboardShortcuts.Name, to item: NSMenuItem) {
        item.setShortcut(for: name)
    }
}
```

- [ ] **Step 2: Remove updateMenuPartner + callers from AppDelegate**

In `Ping/AppDelegate.swift`:

Delete `updateMenuPartner()` function (lines around 670-681).

Remove the following call sites (just the lines, not surrounding code):
- Around line 129 in `bootstrapBackend`: delete `updateMenuPartner()`
- Around line 161 in `startObservers` (`roomObserverTask` body): delete `updateMenuPartner()`
- Around line 634 in `insertOrReplaceRoom`: delete `updateMenuPartner()`

Use Edit tool with `replace_all` true for the call `updateMenuPartner()`:

```
Edit:
old_string: "                updateMenuPartner()\n"
new_string: ""
replace_all: true
```

Then delete the function definition explicitly:

```
Edit:
old_string: |
    private func updateMenuPartner() {
        guard let menu = statusItem?.menu,
              let item = menu.item(withTag: StatusMenuBuilder.partnerItemTag) else {
            return
        }

        if let room = appState.defaultRoom {
            item.title = "파트너: \(partnerName(in: room))"
        } else {
            item.title = "파트너: 없음"
        }
    }

new_string: ""
```

- [ ] **Step 3: Build to verify no broken references**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED.

- [ ] **Step 4: Manual smoke test**

메뉴바 아이콘 클릭 → "파트너: ..." 아이템이 사라졌고 나머지 메뉴는 정상.

- [ ] **Step 5: Commit**

```bash
git add Ping/StatusMenuBuilder.swift Ping/AppDelegate.swift
git commit -m "feat(menu): remove partner display from status menu"
```

---

## Phase B0 — 캡쳐 모드 단축키 분리 (v0.2.1 prerequisite)

기존 plan의 F 토글 + lastCaptureMode + Settings 라디오 패턴 대신 두 글로벌 단축키로 분리.

### Task B0.1: KeyboardShortcuts.Name에 captureScreenFace 추가

**Files:**
- Modify: `Ping/Hotkey/HotkeyManager.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: HotkeyManager에 단축키 정의 추가**

In `Ping/Hotkey/HotkeyManager.swift`:

```swift
extension KeyboardShortcuts.Name {
    static let pingTrigger = Self("pingTrigger", default: .init(.p, modifiers: [.option]))
    static let appearanceToggle = Self("appearanceToggle", default: .init(.d, modifiers: [.option, .shift]))
    static let captureScreenFace = Self("captureScreenFace", default: .init(.l, modifiers: [.option]))
}
```

(`pingTrigger`는 그대로. 의미만 "얼굴만 모드"로 한정. UserDefaults 호환 유지.)

Update `register`:

```swift
    func register(
        onCaptureFace: @escaping () -> Void,
        onAppearanceToggle: @escaping () -> Void,
        onCaptureScreenFace: @escaping () -> Void
    ) {
        KeyboardShortcuts.onKeyDown(for: .pingTrigger) {
            onCaptureFace()
        }
        KeyboardShortcuts.onKeyDown(for: .appearanceToggle) {
            onAppearanceToggle()
        }
        KeyboardShortcuts.onKeyDown(for: .captureScreenFace) {
            onCaptureScreenFace()
        }
    }
```

- [ ] **Step 2: AppDelegate에서 두 콜백 별도 발사**

In `Ping/AppDelegate.swift`, replace `setupHotkey`:

```swift
    private func setupHotkey() {
        HotkeyManager.shared.register(
            onCaptureFace: { [weak self] in self?.toggleMirror(mode: .faceOnly) },
            onAppearanceToggle: { [weak self] in self?.toggleAppearanceMode() },
            onCaptureScreenFace: { [weak self] in self?.toggleMirror(mode: .screenFace) }
        )
    }
```

`toggleMirror`은 mode 파라미터를 받도록 변경:

```swift
    private var currentMirrorMode: CaptureMode?

    private func toggleMirror(mode: CaptureMode) {
        if let _ = mirrorWindow, currentMirrorMode == mode {
            // 같은 모드 단축키 → toggle off
            mirrorWindow?.orderOut(nil)
            mirrorWindow = nil
            currentMirrorMode = nil
            camera.stop()
            return
        }
        if mirrorWindow != nil {
            // 다른 모드 단축키 → 현재 거울 닫고 새 모드로 재시작
            mirrorWindow?.orderOut(nil)
            mirrorWindow = nil
        }
        showMirror(mode: mode)
        currentMirrorMode = mode
    }

    private func showMirror(mode: CaptureMode) {
        // 모드별 윈도우 사이즈/형태 결정 → MirrorWindow 생성 (Phase B 후속 task에서 구체화)
        // 우선 v0.2.1 단계에선 mode를 store만 하고 기존 거울 띄움
        // ...existing showMirror logic, accept mode argument...
    }
```

(Note: CaptureMode enum이 아직 정의 안 됨 — Task B2에서 정의. B0.1에서는 String enum 임시로 정의하거나, B2와 순서 조정. 가장 깔끔하게 B0.1에서 함께 정의.)

- [ ] **Step 3: CaptureMode enum을 Core/Models.swift에 미리 정의**

In `Ping/Core/Models.swift`, add (before VideoMessage):

```swift
enum CaptureMode: String, Codable, Hashable {
    case faceOnly = "face_only"
    case screenFace = "screen_face"
}
```

(VideoMessage에 컬럼 추가는 Task B2에서. 여기선 enum 자체만.)

- [ ] **Step 4: Build to verify**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED.

- [ ] **Step 5: Commit**

```bash
git add Ping/Hotkey/HotkeyManager.swift Ping/AppDelegate.swift Ping/Core/Models.swift
git commit -m "feat(hotkey): split capture into Option+P (face) and Option+L (screen+face)"
```

---

### Task B0.2: Settings 단축키 탭에 captureScreenFace 항목 표시

**Files:**
- Modify: `Ping/UI/Setup/SettingsView.swift` (or wherever the hotkey settings tab lives)

- [ ] **Step 1: 단축키 탭에 항목 추가**

Read current settings view file. Find the section where `pingTrigger` and `appearanceToggle` shortcuts are rendered. Add a new row for `captureScreenFace`:

```swift
KeyboardShortcuts.Recorder("화면+얼굴 거울", name: .captureScreenFace)
```

(Place alongside existing `pingTrigger` and `appearanceToggle` recorders. Korean label aligns with existing UI.)

- [ ] **Step 2: Build and verify shortcut customization**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`

Manual: Settings 열기 → 단축키 탭 → "화면+얼굴 거울" 항목 보이는지 확인. 다른 단축키로 변경 시도해서 정상 저장되는지 확인.

- [ ] **Step 3: Commit**

```bash
git add Ping/UI/Setup/SettingsView.swift
git commit -m "feat(settings): expose captureScreenFace shortcut in settings"
```

---

> **B0 이후 Phase B task 영향 사항** (v0.2.1 amendment 적용):
> - **Task B3**: `UserPreferences.lastCaptureMode` 부분 **삭제**. ScreenCapturePermission만 유지.
> - **Task B8 (F 토글)**: 전부 **삭제**. 모드는 단축키가 결정. `appState.captureMode`는 `toggleMirror(mode:)` 호출 시 set, 또는 mirror window가 mode를 직접 보유.
> - **Task B9, B10**: `appState.captureMode` 대신 `MirrorView`/`MirrorWindow`가 mode를 init 시점에 받는 패턴으로 변경.
> - **Task B13**: "Settings의 캡쳐 모드 라디오" 부분 **삭제**. 권한 fallback은 유지하되, Option+L 단축키 누름 → 권한 없으면 안내 (자동 fallback X). Option+P (얼굴만)는 항상 동작.

## Phase B — 전체화면+얼굴 PIP 캡쳐

### Task B1: Supabase 마이그레이션 — capture_mode/aspect_ratio 컬럼

**Files:**
- Create: `supabase/migrations/20260521000100_v0_2_capture_mode.sql`

- [ ] **Step 1: Write migration**

Create `supabase/migrations/20260521000100_v0_2_capture_mode.sql`:

```sql
-- Phase B: capture mode + aspect ratio for screen+face messages
alter table public.messages
    add column if not exists capture_mode text not null default 'face_only'
        check (capture_mode in ('face_only', 'screen_face'));

alter table public.messages
    add column if not exists aspect_ratio real;

alter table public.messages
    add constraint aspect_ratio_range
        check (aspect_ratio is null or (aspect_ratio > 0 and aspect_ratio <= 8));

-- Update ping_create_message signature
drop function if exists public.ping_create_message(uuid, uuid, text, text, text, double precision, double precision);

create or replace function public.ping_create_message(
    room_uuid uuid,
    receiver_uid uuid,
    sender_nickname_text text,
    video_id_text text,
    video_url_text text,
    x_ratio double precision,
    y_ratio double precision,
    capture_mode_text text default 'face_only',
    aspect_ratio_value real default null
) returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
    sender uuid := auth.uid();
    new_id uuid;
begin
    if sender is null then
        raise exception 'auth required';
    end if;

    if capture_mode_text not in ('face_only', 'screen_face') then
        raise exception 'invalid capture_mode';
    end if;

    insert into public.messages(
        room_id, sender_uid, receiver_uid, sender_nickname,
        video_id, video_url, duration_ms,
        mirror_position, status,
        capture_mode, aspect_ratio
    ) values (
        room_uuid, sender, receiver_uid, sender_nickname_text,
        video_id_text, video_url_text, 3000,
        jsonb_build_object('xRatio', x_ratio, 'yRatio', y_ratio),
        'uploaded',
        capture_mode_text, aspect_ratio_value
    ) returning id into new_id;

    return new_id;
end;
$$;

grant execute on function public.ping_create_message(uuid, uuid, text, text, text, double precision, double precision, text, real) to authenticated;
```

- [ ] **Step 2: Apply migration locally**

Run: `npx supabase db push`
Expected: migration applied without error.

- [ ] **Step 3: Verify columns exist**

Run: `npx supabase db remote query "select column_name from information_schema.columns where table_name='messages' and column_name in ('capture_mode','aspect_ratio')"`
Expected: 2 rows returned.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260521000100_v0_2_capture_mode.sql
git commit -m "feat(db): add capture_mode and aspect_ratio columns to messages"
```

---

### Task B2: VideoMessage 모델에 capture_mode/aspect_ratio 추가 (TDD)

**Files:**
- Modify: `Ping/Core/Models.swift`
- Test: `PingTests/VideoMessageCodingTests.swift` (create)

- [ ] **Step 1: Write failing test for decoding new columns**

Create `PingTests/VideoMessageCodingTests.swift`:

```swift
import XCTest
@testable import Ping

final class VideoMessageCodingTests: XCTestCase {
    func test_decode_screenFaceMessage_withAspectRatio() throws {
        let json = """
        {
          "id": "msg-1",
          "room_id": "room-1",
          "sender_uid": "u1",
          "receiver_uid": "u2",
          "sender_nickname": "alice",
          "video_id": "v1",
          "video_url": "u1/v1.mp4",
          "duration_ms": 2000,
          "mirror_position": {"xRatio": 0.5, "yRatio": 0.5},
          "status": "uploaded",
          "expires_at": "2030-01-01T00:00:00Z",
          "capture_mode": "screen_face",
          "aspect_ratio": 1.7777
        }
        """
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        let msg = try decoder.decode(VideoMessage.self, from: json.data(using: .utf8)!)
        XCTAssertEqual(msg.captureMode, .screenFace)
        XCTAssertEqual(msg.aspectRatio ?? 0, 1.7777, accuracy: 0.0001)
    }

    func test_decode_legacyMessage_defaultsToFaceOnly() throws {
        let json = """
        {
          "id": "msg-1",
          "room_id": "room-1",
          "sender_uid": "u1",
          "receiver_uid": "u2",
          "sender_nickname": "alice",
          "video_id": "v1",
          "video_url": "u1/v1.mp4",
          "duration_ms": 2000,
          "mirror_position": {"xRatio": 0.5, "yRatio": 0.5},
          "status": "uploaded",
          "expires_at": "2030-01-01T00:00:00Z"
        }
        """
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        let msg = try decoder.decode(VideoMessage.self, from: json.data(using: .utf8)!)
        XCTAssertEqual(msg.captureMode, .faceOnly)
        XCTAssertNil(msg.aspectRatio)
    }
}
```

- [ ] **Step 2: Run test to verify it fails (compile error)**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/VideoMessageCodingTests`
Expected: FAIL — `captureMode`/`aspectRatio`/`CaptureMode` not defined.

- [ ] **Step 3: Update VideoMessage and add CaptureMode enum**

In `Ping/Core/Models.swift`, replace `VideoMessage` struct and add `CaptureMode` enum:

```swift
enum CaptureMode: String, Codable, Hashable {
    case faceOnly = "face_only"
    case screenFace = "screen_face"
}

struct VideoMessage: Codable, Identifiable, Hashable {
    var id: String?
    var roomId: String
    var senderUid: String
    var receiverUid: String
    var senderNickname: String
    var videoId: String
    var videoUrl: String
    var durationMs: Int
    var mirrorPosition: MirrorPosition
    var status: MessageStatus
    var createdAt: Date?
    var expiresAt: Date
    var captureMode: CaptureMode
    var aspectRatio: Double?

    enum CodingKeys: String, CodingKey {
        case id
        case roomId = "room_id"
        case senderUid = "sender_uid"
        case receiverUid = "receiver_uid"
        case senderNickname = "sender_nickname"
        case videoId = "video_id"
        case videoUrl = "video_url"
        case durationMs = "duration_ms"
        case mirrorPosition = "mirror_position"
        case status
        case createdAt = "created_at"
        case expiresAt = "expires_at"
        case captureMode = "capture_mode"
        case aspectRatio = "aspect_ratio"
    }

    init(from decoder: Decoder) throws {
        let c = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try c.decodeIfPresent(String.self, forKey: .id)
        self.roomId = try c.decode(String.self, forKey: .roomId)
        self.senderUid = try c.decode(String.self, forKey: .senderUid)
        self.receiverUid = try c.decode(String.self, forKey: .receiverUid)
        self.senderNickname = try c.decode(String.self, forKey: .senderNickname)
        self.videoId = try c.decode(String.self, forKey: .videoId)
        self.videoUrl = try c.decode(String.self, forKey: .videoUrl)
        self.durationMs = try c.decode(Int.self, forKey: .durationMs)
        self.mirrorPosition = try c.decode(MirrorPosition.self, forKey: .mirrorPosition)
        self.status = try c.decode(MessageStatus.self, forKey: .status)
        self.createdAt = try c.decodeIfPresent(Date.self, forKey: .createdAt)
        self.expiresAt = try c.decode(Date.self, forKey: .expiresAt)
        self.captureMode = try c.decodeIfPresent(CaptureMode.self, forKey: .captureMode) ?? .faceOnly
        self.aspectRatio = try c.decodeIfPresent(Double.self, forKey: .aspectRatio)
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/VideoMessageCodingTests`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add Ping/Core/Models.swift PingTests/VideoMessageCodingTests.swift
git commit -m "feat(models): add CaptureMode + aspectRatio to VideoMessage"
```

---

### Task B3: UserPreferences에 capture mode preference + ScreenCapturePermission helper

**Files:**
- Modify: `Ping/Core/UserPreferences.swift`
- Create: `Ping/Capture/ScreenCapturePermission.swift`

- [ ] **Step 1: Add capture mode preference**

In `Ping/Core/UserPreferences.swift`, append (at end of UserPreferences or relevant struct — read file first to see structure, then add):

```swift
extension UserPreferences {
    static let captureModeKey = "ping.preferences.captureMode"

    static var lastCaptureMode: CaptureMode {
        get {
            let raw = UserDefaults.standard.string(forKey: captureModeKey) ?? CaptureMode.screenFace.rawValue
            return CaptureMode(rawValue: raw) ?? .screenFace
        }
        set {
            UserDefaults.standard.set(newValue.rawValue, forKey: captureModeKey)
        }
    }
}
```

- [ ] **Step 2: Create ScreenCapturePermission**

Create `Ping/Capture/ScreenCapturePermission.swift`:

```swift
import AppKit
import ScreenCaptureKit

@MainActor
enum ScreenCapturePermission {
    enum Status {
        case authorized
        case denied
        case unknown
    }

    static func currentStatus() async -> Status {
        do {
            _ = try await SCShareableContent.excludingDesktopWindows(false, onScreenWindowsOnly: true)
            return .authorized
        } catch {
            return .denied
        }
    }

    static func openSystemSettings() {
        let url = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture")!
        NSWorkspace.shared.open(url)
    }
}
```

- [ ] **Step 3: Build to verify compilation**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED.

- [ ] **Step 4: Commit**

```bash
git add Ping/Core/UserPreferences.swift Ping/Capture/ScreenCapturePermission.swift
git commit -m "feat(capture): add capture mode preference and ScreenCapturePermission helper"
```

---

### Task B4: PIPCompositor — CIImage 단계에서 화면+얼굴 PIP 합성 (TDD)

**Files:**
- Create: `Ping/Capture/PIPCompositor.swift`
- Test: `PingTests/PIPCompositorTests.swift`

- [ ] **Step 1: Write failing test**

Create `PingTests/PIPCompositorTests.swift`:

```swift
import XCTest
import CoreImage
@testable import Ping

final class PIPCompositorTests: XCTestCase {
    func test_compose_putsFaceInBottomRightWithCircleMask() {
        let screenSize = CGSize(width: 1920, height: 1080)
        let faceSize = CGSize(width: 1080, height: 1080)
        let screen = CIImage(color: .red).cropped(to: CGRect(origin: .zero, size: screenSize))
        let face = CIImage(color: .green).cropped(to: CGRect(origin: .zero, size: faceSize))

        let composed = PIPCompositor.compose(screen: screen, face: face, outputSize: screenSize, faceDiameterRatio: 0.18, paddingRatio: 0.03)

        XCTAssertEqual(composed.extent.size, screenSize)
        // Sample a point in the bottom-right inside expected face circle: should be green-ish.
        let context = CIContext()
        let cgImage = context.createCGImage(composed, from: composed.extent)!
        let bitmap = NSBitmapImageRep(cgImage: cgImage)
        // Face diameter = 1920 * 0.18 ≈ 345. Padding = 1920 * 0.03 ≈ 57.
        // Face center x = 1920 - 57 - 345/2 ≈ 1690. y = 57 + 345/2 ≈ 230 (CI uses bottom-left origin).
        // NSBitmapImageRep uses top-left for pixelAtX:y:; convert: NSBitmap y_top = height - y_ci = 1080 - 230 = 850.
        let color = bitmap.colorAt(x: 1690, y: 850)!
        XCTAssertGreaterThan(color.greenComponent, 0.5)
        XCTAssertLessThan(color.redComponent, 0.5)

        // Sample top-left: should be red (outside face PIP).
        let color2 = bitmap.colorAt(x: 100, y: 100)!
        XCTAssertGreaterThan(color2.redComponent, 0.5)
    }
}
```

- [ ] **Step 2: Run test to verify it fails (compile)**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/PIPCompositorTests`
Expected: FAIL — `PIPCompositor` not defined.

- [ ] **Step 3: Implement PIPCompositor**

Create `Ping/Capture/PIPCompositor.swift`:

```swift
import CoreImage
import CoreImage.CIFilterBuiltins
import Foundation

enum PIPCompositor {
    static func compose(
        screen: CIImage,
        face: CIImage,
        outputSize: CGSize,
        faceDiameterRatio: CGFloat,
        paddingRatio: CGFloat
    ) -> CIImage {
        let diameter = min(outputSize.width, outputSize.height) * faceDiameterRatio
        let padding = min(outputSize.width, outputSize.height) * paddingRatio

        let scaledFace = scaledToFill(image: face, square: diameter)
        let maskedFace = applyCircularMask(image: scaledFace, diameter: diameter)

        let faceOriginX = outputSize.width - padding - diameter
        let faceOriginY = padding
        let positionedFace = maskedFace.transformed(by: CGAffineTransform(translationX: faceOriginX, y: faceOriginY))

        return positionedFace.composited(over: screen)
    }

    private static func scaledToFill(image: CIImage, square: CGFloat) -> CIImage {
        let extent = image.extent
        let scale = square / min(extent.width, extent.height)
        let scaled = image.transformed(by: CGAffineTransform(scaleX: scale, y: scale))
        let s = scaled.extent
        let cropOriginX = s.origin.x + (s.width - square) / 2
        let cropOriginY = s.origin.y + (s.height - square) / 2
        return scaled
            .cropped(to: CGRect(x: cropOriginX, y: cropOriginY, width: square, height: square))
            .transformed(by: CGAffineTransform(translationX: -cropOriginX, y: -cropOriginY))
    }

    private static func applyCircularMask(image: CIImage, diameter: CGFloat) -> CIImage {
        let mask = CIFilter.radialGradient()
        mask.center = CGPoint(x: diameter / 2, y: diameter / 2)
        mask.radius0 = Float(diameter / 2 - 1)
        mask.radius1 = Float(diameter / 2)
        mask.color0 = CIColor(red: 1, green: 1, blue: 1, alpha: 1)
        mask.color1 = CIColor(red: 0, green: 0, blue: 0, alpha: 0)
        let maskImage = mask.outputImage!.cropped(to: CGRect(x: 0, y: 0, width: diameter, height: diameter))

        let blend = CIFilter.blendWithAlphaMask()
        blend.inputImage = image
        blend.backgroundImage = CIImage(color: CIColor(red: 0, green: 0, blue: 0, alpha: 0))
            .cropped(to: CGRect(x: 0, y: 0, width: diameter, height: diameter))
        blend.maskImage = maskImage
        return blend.outputImage!.cropped(to: CGRect(x: 0, y: 0, width: diameter, height: diameter))
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/PIPCompositorTests`
Expected: PASS.

If the green-detection assertion fails by a small margin, the test sample point may need adjustment — the CI bottom-left origin vs NSBitmap top-left conversion is the easy source of off-by-one. Print computed `colorAt` values to validate.

- [ ] **Step 5: Commit**

```bash
git add Ping/Capture/PIPCompositor.swift PingTests/PIPCompositorTests.swift
git commit -m "feat(capture): add PIPCompositor for screen+face composition"
```

---

### Task B5: ScreenCaptureManager — SCStream wrapper for live preview & recording

**Files:**
- Create: `Ping/Capture/ScreenCaptureManager.swift`

- [ ] **Step 1: Write ScreenCaptureManager**

Create `Ping/Capture/ScreenCaptureManager.swift`:

```swift
import AppKit
@preconcurrency import ScreenCaptureKit
import Combine
import CoreImage

@MainActor
final class ScreenCaptureManager: NSObject, ObservableObject {
    @Published private(set) var latestFrame: CIImage?
    @Published private(set) var lastError: String?

    private var stream: SCStream?
    private let output = FrameOutput()

    func startPreview(on screen: NSScreen) async {
        await start(on: screen, frameRateHz: 24)
    }

    func startRecording(on screen: NSScreen) async {
        await start(on: screen, frameRateHz: 30)
    }

    func stop() async {
        guard let stream else { return }
        try? await stream.stopCapture()
        self.stream = nil
    }

    private func start(on screen: NSScreen, frameRateHz: Int) async {
        do {
            let content = try await SCShareableContent.excludingDesktopWindows(false, onScreenWindowsOnly: true)
            guard let display = content.displays.first(where: { $0.frame == screen.frame.integral }) ?? content.displays.first else {
                lastError = "디스플레이를 찾지 못함"
                return
            }

            let pid = ProcessInfo.processInfo.processIdentifier
            let myApp = content.applications.first { $0.processID == pid }
            let exclusions = myApp.map { [$0] } ?? []

            let filter = SCContentFilter(display: display, excludingApplications: exclusions, exceptingWindows: [])
            let config = SCStreamConfiguration()
            config.width = Int(display.width)
            config.height = Int(display.height)
            config.minimumFrameInterval = CMTime(value: 1, timescale: Int32(frameRateHz))
            config.queueDepth = 5
            config.showsCursor = true

            let stream = SCStream(filter: filter, configuration: config, delegate: nil)
            try stream.addStreamOutput(output, type: .screen, sampleHandlerQueue: .global(qos: .userInteractive))
            output.onFrame = { [weak self] image in
                Task { @MainActor in self?.latestFrame = image }
            }
            try await stream.startCapture()
            self.stream = stream
            self.lastError = nil
        } catch {
            self.lastError = error.localizedDescription
        }
    }

    final class FrameOutput: NSObject, SCStreamOutput {
        nonisolated(unsafe) var onFrame: ((CIImage) -> Void)?

        func stream(_ stream: SCStream, didOutputSampleBuffer sampleBuffer: CMSampleBuffer, of type: SCStreamOutputType) {
            guard type == .screen, sampleBuffer.isValid,
                  let pixelBuffer = sampleBuffer.imageBuffer else { return }
            let image = CIImage(cvPixelBuffer: pixelBuffer)
            onFrame?(image)
        }
    }
}
```

- [ ] **Step 2: Build to verify**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED.

If build fails with "SCStream module not found", confirm `import ScreenCaptureKit` is present and deployment target supports macOS 13+.

- [ ] **Step 3: Commit**

```bash
git add Ping/Capture/ScreenCaptureManager.swift
git commit -m "feat(capture): add ScreenCaptureManager for live preview and recording"
```

---

### Task B6: ScreenFaceRecorder — 두 stream 합성 + AVAssetWriter MP4 인코딩

**Files:**
- Create: `Ping/Capture/ScreenFaceRecorder.swift`

- [ ] **Step 1: Implement ScreenFaceRecorder**

Create `Ping/Capture/ScreenFaceRecorder.swift`:

```swift
@preconcurrency import AVFoundation
@preconcurrency import ScreenCaptureKit
import CoreImage
import CoreVideo

@MainActor
final class ScreenFaceRecorder: NSObject {
    struct Output {
        let url: URL
        let aspectRatio: Double
    }

    func record(
        screenManager: ScreenCaptureManager,
        cameraSession: AVCaptureSession,
        screenSize: CGSize,
        duration: TimeInterval = 2.0
    ) async throws -> Output {
        let outputURL = FileManager.default.temporaryDirectory
            .appendingPathComponent("ping-sf-\(UUID().uuidString).mp4")
        try? FileManager.default.removeItem(at: outputURL)

        let longSide: CGFloat = 720
        let scale = longSide / max(screenSize.width, screenSize.height)
        let outW = Int((screenSize.width * scale).rounded())
        let outH = Int((screenSize.height * scale).rounded())
        let outputSize = CGSize(width: outW, height: outH)

        let writer = try AVAssetWriter(outputURL: outputURL, fileType: .mp4)

        let videoSettings: [String: Any] = [
            AVVideoCodecKey: AVVideoCodecType.h264,
            AVVideoWidthKey: outW,
            AVVideoHeightKey: outH,
            AVVideoCompressionPropertiesKey: [
                AVVideoAverageBitRateKey: 4_000_000,
                AVVideoExpectedSourceFrameRateKey: 30,
                AVVideoMaxKeyFrameIntervalKey: 30
            ]
        ]

        let writerInput = AVAssetWriterInput(mediaType: .video, outputSettings: videoSettings)
        writerInput.expectsMediaDataInRealTime = true

        let adaptor = AVAssetWriterInputPixelBufferAdaptor(
            assetWriterInput: writerInput,
            sourcePixelBufferAttributes: [
                kCVPixelBufferPixelFormatTypeKey as String: kCVPixelFormatType_32BGRA,
                kCVPixelBufferWidthKey as String: outW,
                kCVPixelBufferHeightKey as String: outH
            ]
        )

        writer.add(writerInput)
        writer.startWriting()
        writer.startSession(atSourceTime: .zero)

        let ciContext = CIContext()
        let start = Date()
        let frameInterval: TimeInterval = 1.0 / 30.0
        var frameIndex: Int64 = 0

        while Date().timeIntervalSince(start) < duration {
            while !writerInput.isReadyForMoreMediaData {
                try? await Task.sleep(for: .milliseconds(5))
            }

            guard let screenImage = screenManager.latestFrame else {
                try? await Task.sleep(for: .milliseconds(10))
                continue
            }
            guard let cameraImage = await currentCameraFrame(session: cameraSession) else {
                try? await Task.sleep(for: .milliseconds(10))
                continue
            }

            let scaledScreen = screenImage.transformed(by: CGAffineTransform(scaleX: outputSize.width / screenImage.extent.width, y: outputSize.height / screenImage.extent.height))
            let composed = PIPCompositor.compose(
                screen: scaledScreen,
                face: cameraImage,
                outputSize: outputSize,
                faceDiameterRatio: 72.0 / longSide,
                paddingRatio: 12.0 / longSide
            )

            var pbOpt: CVPixelBuffer?
            CVPixelBufferCreate(nil, outW, outH, kCVPixelFormatType_32BGRA, [
                kCVPixelBufferCGImageCompatibilityKey: true,
                kCVPixelBufferCGBitmapContextCompatibilityKey: true
            ] as CFDictionary, &pbOpt)
            guard let pb = pbOpt else { continue }
            ciContext.render(composed, to: pb)

            let presentation = CMTime(value: frameIndex, timescale: 30)
            adaptor.append(pb, withPresentationTime: presentation)
            frameIndex += 1
            try? await Task.sleep(for: .seconds(frameInterval))
        }

        writerInput.markAsFinished()
        await writer.finishWriting()

        if writer.status != .completed {
            throw writer.error ?? NSError(domain: "ScreenFaceRecorder", code: -1)
        }

        let aspect = Double(outW) / Double(outH)
        return Output(url: outputURL, aspectRatio: aspect)
    }

    private func currentCameraFrame(session: AVCaptureSession) async -> CIImage? {
        // The existing CameraManager exposes a session with AVCaptureVideoDataOutput.
        // We piggyback on its latestVideoFrame property (added in Task B7 below).
        return CameraManager.latestVideoFrame
    }
}
```

- [ ] **Step 2: Build to verify**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD FAILED — `CameraManager.latestVideoFrame` not defined. This is intentional, fixed in Task B7.

- [ ] **Step 3: Commit (WIP)**

```bash
git add Ping/Capture/ScreenFaceRecorder.swift
git commit -m "feat(capture): scaffold ScreenFaceRecorder (camera hook pending)"
```

---

### Task B7: CameraManager — expose latest video frame for compositing

**Files:**
- Modify: `Ping/Capture/CameraManager.swift`

- [ ] **Step 1: Read current CameraManager**

Run: `cat Ping/Capture/CameraManager.swift`
Note: identify current output (`AVCaptureMovieFileOutput` only) and adjust.

- [ ] **Step 2: Add AVCaptureVideoDataOutput parallel to movie output**

In `Ping/Capture/CameraManager.swift`, add:

```swift
// At the top of the file, alongside existing properties:
    nonisolated(unsafe) static var latestVideoFrame: CIImage?

    private let videoDataOutput = AVCaptureVideoDataOutput()
    private let videoFrameQueue = DispatchQueue(label: "ping.camera.frame")
```

In the `start()` method (or wherever the capture session is configured), after `session.addOutput(movieOutput)`, add:

```swift
        videoDataOutput.alwaysDiscardsLateVideoFrames = true
        videoDataOutput.videoSettings = [
            kCVPixelBufferPixelFormatTypeKey as String: kCVPixelFormatType_32BGRA
        ]
        videoDataOutput.setSampleBufferDelegate(self, queue: videoFrameQueue)
        if session.canAddOutput(videoDataOutput) {
            session.addOutput(videoDataOutput)
        }
```

Conform `CameraManager` to `AVCaptureVideoDataOutputSampleBufferDelegate`:

```swift
extension CameraManager: AVCaptureVideoDataOutputSampleBufferDelegate {
    nonisolated func captureOutput(
        _ output: AVCaptureOutput,
        didOutput sampleBuffer: CMSampleBuffer,
        from connection: AVCaptureConnection
    ) {
        guard let pb = sampleBuffer.imageBuffer else { return }
        CameraManager.latestVideoFrame = CIImage(cvPixelBuffer: pb)
    }
}
```

- [ ] **Step 3: Build to verify**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED (B6's reference to `CameraManager.latestVideoFrame` now resolves).

- [ ] **Step 4: Commit**

```bash
git add Ping/Capture/CameraManager.swift
git commit -m "feat(capture): expose latestVideoFrame on CameraManager for PIP composition"
```

---

### Task B8: Mirror window/view에 모드별 형태 + F 토글

**Files:**
- Modify: `Ping/UI/Mirror/MirrorWindow.swift`
- Modify: `Ping/UI/Mirror/MirrorView.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Add mode-aware window sizing**

Replace `Ping/UI/Mirror/MirrorWindow.swift` entirely:

```swift
import AppKit
import SwiftUI

@MainActor
final class MirrorWindow: NSWindow {
    static let faceOnlySize = NSSize(width: 200, height: 200)

    static func sizeForScreenFace(on screen: NSScreen) -> NSSize {
        let aspect = screen.frame.width / screen.frame.height
        let longSide: CGFloat = 480
        if aspect >= 1 {
            return NSSize(width: longSide, height: (longSide / aspect).rounded())
        } else {
            return NSSize(width: (longSide * aspect).rounded(), height: longSide)
        }
    }

    init(initialSize: NSSize, origin: NSPoint) {
        super.init(
            contentRect: NSRect(origin: origin, size: initialSize),
            styleMask: [.borderless],
            backing: .buffered,
            defer: false
        )
        isOpaque = false
        backgroundColor = .clear
        level = .floating
        hasShadow = false
        collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        isReleasedWhenClosed = false
        sharingType = .none
    }

    func resize(to newSize: NSSize, animated: Bool) {
        let origin = NSPoint(
            x: frame.midX - newSize.width / 2,
            y: frame.midY - newSize.height / 2
        )
        let newFrame = NSRect(origin: origin, size: newSize)
        if animated {
            NSAnimationContext.runAnimationGroup { ctx in
                ctx.duration = 0.25
                animator().setFrame(newFrame, display: true)
            }
        } else {
            setFrame(newFrame, display: true)
        }
    }
}
```

- [ ] **Step 2: Update MirrorView body shape per mode + add F toggle**

In `Ping/UI/Mirror/MirrorView.swift`, add capture mode binding and mode-aware shape:

Add `@ObservedObject var screenCapture: ScreenCaptureManager` to MirrorView struct (next to other ObservedObjects), and pass it through MirrorWindow creation in AppDelegate.

In MirrorView body, branch the outer shape:

```swift
    var body: some View {
        ZStack {
            mirrorShadowSurface
            mirrorContent
        }
        .frame(width: contentSize.width, height: contentSize.height)
        .contentShape(currentShape)
        .onAppear {
            selectedRoomId = appState.defaultRoom?.id
            installKeyMonitor()
        }
        .onDisappear {
            if let keyMonitor {
                NSEvent.removeMonitor(keyMonitor)
            }
            keyMonitor = nil
        }
    }

    private var contentSize: CGSize {
        switch appState.captureMode {
        case .faceOnly: return CGSize(width: 200, height: 200)
        case .screenFace:
            let screen = NSScreen.main ?? NSScreen.screens.first!
            let size = MirrorWindow.sizeForScreenFace(on: screen)
            return CGSize(width: size.width, height: size.height)
        }
    }

    @ViewBuilder private var currentShape: some Shape {
        switch appState.captureMode {
        case .faceOnly: Circle()
        case .screenFace: RoundedRectangle(cornerRadius: 16)
        }
    }
```

(`appState.captureMode` and a `setCaptureMode` method must be added to AppState — see next step.)

- [ ] **Step 3: Add captureMode to AppState**

In `Ping/Core/AppState.swift`, add:

```swift
    @Published var captureMode: CaptureMode = UserPreferences.lastCaptureMode {
        didSet {
            UserPreferences.lastCaptureMode = captureMode
        }
    }
```

- [ ] **Step 4: Add F key handler in MirrorView**

In `Ping/UI/Mirror/MirrorView.swift`, inside `installKeyMonitor`'s switch, add:

```swift
            case 3: // F
                handleModeToggle()
                return nil
```

Then add the method:

```swift
    private func handleModeToggle() {
        switch appState.captureMode {
        case .faceOnly:
            Task {
                let status = await ScreenCapturePermission.currentStatus()
                if status == .authorized {
                    appState.captureMode = .screenFace
                } else {
                    // Open Settings instead of silent failure
                    ScreenCapturePermission.openSystemSettings()
                }
            }
        case .screenFace:
            appState.captureMode = .faceOnly
        }
    }
```

- [ ] **Step 5: Build and run smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

Open app, Option+P → 거울이 모니터 비율의 라운드 사각형으로 떠야 함 (현재 화면 라이브 프리뷰는 아직 다음 task에서). F 누르면 200 원형으로 토글.

- [ ] **Step 6: Commit**

```bash
git add Ping/UI/Mirror/MirrorWindow.swift Ping/UI/Mirror/MirrorView.swift Ping/Core/AppState.swift
git commit -m "feat(mirror): mode-aware window shape with F toggle"
```

---

### Task B9: Mirror live preview shows screen + face PIP

**Files:**
- Modify: `Ping/UI/Mirror/MirrorView.swift`

- [ ] **Step 1: Add screen-mode preview layer**

In `Ping/UI/Mirror/MirrorView.swift`, replace `previewLayer`:

```swift
    @ViewBuilder private var previewLayer: some View {
        if case .reviewing(let url) = viewModel.state {
            ReviewLoopPlayerView(url: url)
        } else {
            switch appState.captureMode {
            case .faceOnly:
                CameraPreviewView(session: camera.session)
            case .screenFace:
                ScreenFacePreview(screenCapture: screenCapture, camera: camera)
            }
        }
    }
```

Add `ScreenFacePreview` view at end of file:

```swift
struct ScreenFacePreview: View {
    @ObservedObject var screenCapture: ScreenCaptureManager
    @ObservedObject var camera: CameraManager

    var body: some View {
        ZStack(alignment: .bottomTrailing) {
            ScreenLiveImageView(screenCapture: screenCapture)
            CameraPreviewView(session: camera.session)
                .frame(width: 72, height: 72)
                .clipShape(Circle())
                .padding(12)
        }
    }
}

struct ScreenLiveImageView: NSViewRepresentable {
    @ObservedObject var screenCapture: ScreenCaptureManager

    func makeNSView(context: Context) -> NSImageView {
        let v = NSImageView()
        v.imageScaling = .scaleProportionallyUpOrDown
        return v
    }

    func updateNSView(_ nsView: NSImageView, context: Context) {
        guard let frame = screenCapture.latestFrame else { return }
        let context = CIContext()
        if let cgImage = context.createCGImage(frame, from: frame.extent) {
            nsView.image = NSImage(cgImage: cgImage, size: NSSize(width: cgImage.width, height: cgImage.height))
        }
    }
}
```

- [ ] **Step 2: Wire screen capture lifecycle in AppDelegate when showing mirror**

In `Ping/AppDelegate.swift`, find `toggleMirror()` or `showMirror()` (whatever opens MirrorWindow). When opening, instantiate ScreenCaptureManager and inject:

Read `AppDelegate.swift` line around 180-260 for current toggleMirror. Then update:

```swift
    private let screenCapture = ScreenCaptureManager()

    private func toggleMirror() {
        if let mirrorWindow {
            mirrorWindow.orderOut(nil)
            self.mirrorWindow = nil
            camera.stop()
            Task { await screenCapture.stop() }
            return
        }
        showMirror()
    }

    private func showMirror() {
        let mode = appState.captureMode
        let screen = NSScreen.main ?? NSScreen.screens.first!
        let initialSize: NSSize = (mode == .faceOnly) ? MirrorWindow.faceOnlySize : MirrorWindow.sizeForScreenFace(on: screen)
        let origin = WindowPositioning.savedMirrorOrigin(for: initialSize, on: screen)

        let window = MirrorWindow(initialSize: initialSize, origin: origin)
        let rootView = MirrorView(
            camera: camera,
            screenCapture: screenCapture,
            viewModel: mirrorViewModel,
            appState: appState,
            windowOrigin: { [weak window] in window?.frame.origin ?? .zero },
            onClose: { [weak self] in self?.toggleMirror() },
            onSend: { [weak self] url, position, rooms in
                try await self?.sendVideo(url: url, position: position, rooms: rooms)
            }
        )
        window.contentView = NSHostingView(rootView: rootView)
        window.makeKeyAndOrderFront(nil)
        mirrorWindow = window

        Task {
            await camera.start()
            if mode == .screenFace {
                await screenCapture.startPreview(on: screen)
            }
        }
    }
```

Also observe captureMode changes to start/stop ScreenCaptureManager:

```swift
    // In setupHotkey or applicationDidFinishLaunching:
    Task { @MainActor [weak self] in
        guard let self else { return }
        for await mode in appState.$captureMode.values {
            guard let window = self.mirrorWindow else { continue }
            let screen = window.screen ?? NSScreen.main ?? NSScreen.screens.first!
            let newSize: NSSize = (mode == .faceOnly) ? MirrorWindow.faceOnlySize : MirrorWindow.sizeForScreenFace(on: screen)
            window.resize(to: newSize, animated: true)
            if mode == .screenFace {
                await self.screenCapture.startPreview(on: screen)
            } else {
                await self.screenCapture.stop()
            }
        }
    }
```

- [ ] **Step 3: Build and run smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
DERIVED=$(xcodebuild -project Ping.xcodeproj -scheme Ping -showBuildSettings | grep -m1 BUILT_PRODUCTS_DIR | awk '{print $3}')
open "$DERIVED/Ping.app"
```

Option+P → 모니터 비율 박스에 화면 라이브 + 우하단 얼굴 원형 PIP. F 토글 시 200 원형 ↔ 라운드 사각형 전환.

- [ ] **Step 4: Commit**

```bash
git add Ping/UI/Mirror/MirrorView.swift Ping/AppDelegate.swift
git commit -m "feat(mirror): live screen+face PIP preview wired into AppDelegate"
```

---

### Task B10: Recording branches to ScreenFaceRecorder when in screen_face mode

**Files:**
- Modify: `Ping/UI/Mirror/MirrorView.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Add mode-aware recorder selection in startRecording**

In `Ping/UI/Mirror/MirrorView.swift`, replace `startRecording()`:

```swift
    private func startRecording() async {
        switch viewModel.state {
        case .idle, .reviewing:
            break
        case .failed:
            viewModel.reset()
        case .recording, .uploading:
            return
        }

        if !camera.isReady {
            await camera.start()
            if !camera.isReady {
                viewModel.state = .failed(camera.lastError ?? "카메라 준비 중")
                return
            }
        }

        if targetsResolvingFailed() { return }

        viewModel.state = .recording
        viewModel.countdown = 2

        await camera.prepareAudioForRecording()

        let countdownTask = Task { @MainActor in
            try? await Task.sleep(for: .seconds(1))
            viewModel.countdown = 1
        }

        do {
            let recordedURL: URL
            switch appState.captureMode {
            case .faceOnly:
                let recorder = VideoRecorder(output: camera.movieOutput)
                recordedURL = try await recorder.recordTwoSeconds()
                lastRecordedAspect = 1.0
                lastRecordedMode = .faceOnly
            case .screenFace:
                let screen = NSScreen.main ?? NSScreen.screens.first!
                await screenCapture.startRecording(on: screen)
                let recorder = ScreenFaceRecorder()
                let out = try await recorder.record(
                    screenManager: screenCapture,
                    cameraSession: camera.session,
                    screenSize: screen.frame.size
                )
                recordedURL = out.url
                lastRecordedAspect = out.aspectRatio
                lastRecordedMode = .screenFace
                await screenCapture.startPreview(on: screen)
            }
            countdownTask.cancel()
            viewModel.enterReviewing(url: recordedURL)
        } catch {
            countdownTask.cancel()
            viewModel.state = .failed(error.localizedDescription)
        }
    }

    @State private var lastRecordedAspect: Double = 1.0
    @State private var lastRecordedMode: CaptureMode = .faceOnly
```

- [ ] **Step 2: Pass capture mode + aspect through onSend**

Update the `onSend` signature in MirrorView struct:

```swift
    var onSend: (URL, MirrorPosition, [Room], CaptureMode, Double) async throws -> Void
```

Update `uploadReviewedClip`:

```swift
    private func uploadReviewedClip(url: URL) async {
        viewModel.beginUpload()

        let origin = windowOrigin()
        let screen = NSScreen.main?.frame ?? .zero
        let center = NSPoint(x: origin.x + 100, y: origin.y + 100)
        let position = ScreenCoordinates.normalize(point: center, in: screen)

        do {
            try await onSend(url, position, currentTargets(), lastRecordedMode, lastRecordedAspect)
            try? await Task.sleep(for: .milliseconds(300))
            onClose()
            viewModel.reset()
            appState.sendMode = .singlePartner
        } catch {
            viewModel.state = .failed(error.localizedDescription)
        }
    }
```

- [ ] **Step 3: Update MessageService.send signature + RPC call**

In `Ping/Backend/MessageService.swift`, update `SendInput`:

```swift
    struct SendInput {
        let rooms: [Room]
        let localVideoURL: URL
        let mirrorPosition: MirrorPosition
        let senderUid: String
        let senderNickname: String
        let captureMode: CaptureMode
        let aspectRatio: Double
    }
```

In `send` function, update RPC call body:

```swift
                let _: String = try await client.rpcValue("ping_create_message", body: [
                    "room_uuid": roomId,
                    "receiver_uid": receiverUid,
                    "sender_nickname_text": input.senderNickname,
                    "video_id_text": sharedVideoId,
                    "video_url_text": videoStoragePath,
                    "x_ratio": input.mirrorPosition.xRatio,
                    "y_ratio": input.mirrorPosition.yRatio,
                    "capture_mode_text": input.captureMode.rawValue,
                    "aspect_ratio_value": input.aspectRatio
                ])
```

- [ ] **Step 4: Update AppDelegate.sendVideo signature**

In `Ping/AppDelegate.swift`, find `sendVideo` (around line 270-310). Update signature to accept mode + aspect, and forward to MessageService.SendInput.

- [ ] **Step 5: Build and run end-to-end smoke**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

Option+P → 화면 모드 발사 → reviewing → Enter → 다른 Mac/계정에서 수신 → 아직 PlaybackWindow는 face_only 폼이지만 영상은 화면+얼굴 컨텐츠. 형태 적응은 Task B11에서.

- [ ] **Step 6: Commit**

```bash
git add Ping/UI/Mirror/MirrorView.swift Ping/Backend/MessageService.swift Ping/AppDelegate.swift
git commit -m "feat(capture): branch to ScreenFaceRecorder in screen_face mode"
```

---

### Task B11: PlaybackWindow accepts CaptureMode + aspect_ratio for shape

**Files:**
- Modify: `Ping/UI/Playback/PlaybackWindow.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Parameterize PlaybackWindow shape**

In `Ping/UI/Playback/PlaybackWindow.swift`, update init and add a static sizing helper:

```swift
    static func size(for mode: CaptureMode, aspectRatio: Double?, on screen: NSScreen) -> NSSize {
        switch mode {
        case .faceOnly:
            return NSSize(width: 200, height: 200)
        case .screenFace:
            let aspect = aspectRatio ?? (screen.frame.width / screen.frame.height)
            let longSide: CGFloat = 480
            if aspect >= 1 {
                return NSSize(width: longSide, height: (longSide / aspect).rounded())
            } else {
                return NSSize(width: (longSide * aspect).rounded(), height: longSide)
            }
        }
    }
```

Replace init signature to take mode + aspect, and adjust clipShape:

```swift
    private let captureMode: CaptureMode
    private let aspectRatio: Double?

    init(
        videoURL: URL,
        mode: CaptureMode,
        aspectRatio: Double?,
        atScreenPoint origin: NSPoint,
        screen: NSScreen,
        onFirstPlayEnd: @escaping @MainActor @Sendable () -> Void,
        onDone: @escaping @MainActor @Sendable () -> Void
    ) {
        self.captureMode = mode
        self.aspectRatio = aspectRatio
        self.onDone = onDone
        self.onFirstPlayEnd = onFirstPlayEnd

        let size = Self.size(for: mode, aspectRatio: aspectRatio, on: screen)
        super.init(
            contentRect: NSRect(origin: origin, size: size),
            styleMask: [.borderless],
            backing: .buffered,
            defer: false
        )
        // ... rest same as before ...

        let root = ZStack {
            PlaybackView(url: videoURL, onFirstPlayEnd: { [weak self] in
                self?.handleFirstPlayEnd()
            }, controllerBox: controllerBox)
            .clipShape(currentShapeReference)

            currentShapeReference.strokeBorder(Color.white.opacity(0.30), lineWidth: 1)
        }
        .frame(width: size.width, height: size.height)
        // ... rest same as before ...
    }

    private var currentShapeReference: some Shape {
        switch captureMode {
        case .faceOnly: return AnyShape(Circle())
        case .screenFace: return AnyShape(RoundedRectangle(cornerRadius: 16))
        }
    }
```

(If `AnyShape` is not available in target OS, fall back to a conditional clipShape wrapper view.)

- [ ] **Step 2: Update AppDelegate.playMessage to pass mode/aspect**

In `Ping/AppDelegate.swift`, replace the PlaybackWindow construction in `playMessage`:

```swift
                let screen = NSScreen.main ?? NSScreen.screens.first!
                let size = PlaybackWindow.size(for: message.captureMode, aspectRatio: message.aspectRatio, on: screen)
                let center = ScreenCoordinates.denormalize(position: message.mirrorPosition, in: screen.visibleFrame)
                let origin = ScreenCoordinates.clamp(
                    point: NSPoint(x: center.x - size.width / 2, y: center.y - size.height / 2),
                    windowSize: size,
                    inSafeArea: screen.visibleFrame
                )

                let windowId = UUID()
                let window = PlaybackWindow(
                    videoURL: localURL,
                    mode: message.captureMode,
                    aspectRatio: message.aspectRatio,
                    atScreenPoint: origin,
                    screen: screen,
                    onFirstPlayEnd: { [weak self] in
                        Task { @MainActor in
                            try? await self?.messageService.markSeen(messageId: messageId)
                        }
                    },
                    onDone: { [weak self] in
                        Task { @MainActor in
                            if !LocalArchive.saveReceivedEnabled {
                                try? FileManager.default.removeItem(at: localURL)
                                self?.playbackCache[messageId] = nil
                            }
                            self?.playbackWindows.removeAll { $0.pingWindowId == windowId }
                        }
                    }
                )
                window.pingWindowId = windowId
                playbackWindows.append(window)
                window.fadeIn()
```

- [ ] **Step 3: Build and end-to-end smoke**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

화면 모드 발사 → 수신 → spatial overlay가 모니터 비율 라운드 사각형으로 뜸. Enter 재재생 / Esc 닫기 동작.

- [ ] **Step 4: Commit**

```bash
git add Ping/UI/Playback/PlaybackWindow.swift Ping/AppDelegate.swift
git commit -m "feat(playback): mode-aware playback window shape from server columns"
```

---

### Task B12: 확대 모달 (Space 토글)

**Files:**
- Create: `Ping/UI/Playback/ExpandedPlaybackWindow.swift`
- Modify: `Ping/UI/Playback/PlaybackWindow.swift`

- [ ] **Step 1: Create ExpandedPlaybackWindow**

Create `Ping/UI/Playback/ExpandedPlaybackWindow.swift`:

```swift
import AppKit
import SwiftUI
import AVKit

@MainActor
final class ExpandedPlaybackWindow: NSWindow {
    private let onDismiss: @MainActor @Sendable () -> Void
    private var keyMonitor: Any?

    init(player: AVPlayer, aspectRatio: Double, on screen: NSScreen, onDismiss: @escaping @MainActor @Sendable () -> Void) {
        self.onDismiss = onDismiss

        let frameRect = screen.visibleFrame
        let shortSide = min(frameRect.width, frameRect.height) * 0.8
        let size: NSSize
        if aspectRatio >= 1 {
            size = NSSize(width: shortSide * aspectRatio, height: shortSide)
        } else {
            size = NSSize(width: shortSide, height: shortSide / aspectRatio)
        }
        let origin = NSPoint(
            x: frameRect.midX - size.width / 2,
            y: frameRect.midY - size.height / 2
        )

        super.init(
            contentRect: NSRect(origin: origin, size: size),
            styleMask: [.borderless],
            backing: .buffered,
            defer: false
        )
        isOpaque = false
        backgroundColor = .clear
        level = .floating
        collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        isReleasedWhenClosed = false

        let root = ZStack {
            AVPlayerViewRepresentable(player: player)
                .clipShape(RoundedRectangle(cornerRadius: 16))
            RoundedRectangle(cornerRadius: 16).strokeBorder(Color.white.opacity(0.30), lineWidth: 1)
        }
        let host = NSHostingView(rootView: root)
        host.frame = NSRect(origin: .zero, size: size)
        contentView = host
    }

    func present() {
        makeKeyAndOrderFront(nil)
        keyMonitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { [weak self] event in
            guard let self, event.window === self else { return event }
            switch event.keyCode {
            case 49: // Space
                self.dismiss()
                return nil
            case 53: // Esc
                self.dismiss()
                return nil
            case 36: // Enter
                self.player?.seek(to: .zero)
                self.player?.play()
                return nil
            default:
                return event
            }
        }
    }

    private var player: AVPlayer? {
        (contentView?.subviews.first { $0 is NSView } as? NSView)?.subviews.first { $0.layer is AVPlayerLayer }
            .flatMap { ($0.layer as? AVPlayerLayer)?.player }
    }

    private func dismiss() {
        if let keyMonitor {
            NSEvent.removeMonitor(keyMonitor)
            self.keyMonitor = nil
        }
        orderOut(nil)
        onDismiss()
    }
}

struct AVPlayerViewRepresentable: NSViewRepresentable {
    let player: AVPlayer
    func makeNSView(context: Context) -> NSView {
        let view = NSView()
        view.wantsLayer = true
        let layer = AVPlayerLayer(player: player)
        layer.videoGravity = .resizeAspect
        layer.frame = view.bounds
        view.layer?.addSublayer(layer)
        return view
    }
    func updateNSView(_ nsView: NSView, context: Context) {
        if let layer = nsView.layer?.sublayers?.first as? AVPlayerLayer {
            layer.frame = nsView.bounds
        }
    }
}
```

- [ ] **Step 2: Add Space toggle in PlaybackWindow**

In `Ping/UI/Playback/PlaybackWindow.swift`, add Space case to key monitor and expand logic:

```swift
            case 49: // Space
                self.handleExpand()
                return nil
```

Add `handleExpand` method:

```swift
    private var expandedWindow: ExpandedPlaybackWindow?

    private func handleExpand() {
        guard let player = controllerBox.player, let screen = self.screen ?? NSScreen.main else { return }
        let expanded = ExpandedPlaybackWindow(
            player: player,
            aspectRatio: aspectRatio ?? 1.0,
            on: screen,
            onDismiss: { [weak self] in
                self?.expandedWindow = nil
                self?.makeKeyAndOrderFront(nil)
            }
        )
        self.expandedWindow = expanded
        self.orderOut(nil)
        expanded.present()
    }
```

- [ ] **Step 3: Build and smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

화면 모드 영상 수신 → spatial overlay → Space → 확대 모달 → Esc 또는 Space → 다시 spatial overlay 또는 닫힘.

- [ ] **Step 4: Commit**

```bash
git add Ping/UI/Playback/ExpandedPlaybackWindow.swift Ping/UI/Playback/PlaybackWindow.swift
git commit -m "feat(playback): Space toggle for expanded modal view"
```

---

### Task B13: 권한 거부 → 얼굴 only fallback + Settings 진입점

**Files:**
- Modify: `Ping/UI/Mirror/MirrorView.swift`
- Modify: `Ping/UI/Setup/SettingsView.swift` (or equivalent settings UI)

- [ ] **Step 1: Add permission check on mirror open**

In `Ping/AppDelegate.swift`'s `showMirror`, before launching screenCapture in `screenFace` mode, check permission:

```swift
        Task {
            await camera.start()
            if mode == .screenFace {
                let status = await ScreenCapturePermission.currentStatus()
                if status == .authorized {
                    await screenCapture.startPreview(on: screen)
                } else {
                    appState.captureMode = .faceOnly
                }
            }
        }
```

- [ ] **Step 2: Add "기본 캡쳐 모드" + 권한 안내 to Settings**

Read current `Ping/UI/Setup/SettingsView.swift`, find general tab. Add a new section:

```swift
Section("기본 캡쳐 모드") {
    Picker("모드", selection: $appState.captureMode) {
        Text("얼굴만").tag(CaptureMode.faceOnly)
        Text("화면+얼굴").tag(CaptureMode.screenFace)
    }
    .pickerStyle(.segmented)

    if appState.captureMode == .screenFace {
        HStack {
            Text("화면 녹화 권한이 필요합니다.")
                .font(.caption)
            Spacer()
            Button("시스템 설정 열기") {
                ScreenCapturePermission.openSystemSettings()
            }
        }
    }
}
```

- [ ] **Step 3: Build and smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

권한 거부 상태에서 screenFace 모드 시도 → 자동으로 faceOnly로 fallback. Settings에서 모드 변경 + 권한 안내.

- [ ] **Step 4: Commit**

```bash
git add Ping/AppDelegate.swift Ping/UI/Setup/SettingsView.swift
git commit -m "feat(capture): permission fallback to face_only + Settings entry"
```

---

### Task B14: Phase B 회귀 검증

- [ ] **Step 1: Full test run**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`
Expected: All tests pass.

- [ ] **Step 2: End-to-end matrix smoke**

수동 검증:
- faceOnly → reviewing → Backspace → 재녹화 → Enter → 발사 → 수신 200 원형 → Enter 재재생 → Esc 닫기.
- screenFace → reviewing → Enter → 발사 → 수신 라운드 사각형 → Space 확대 → Esc → 다시 spatial → Esc 닫기.
- F 토글 양방향 동작.
- 권한 거부 시 faceOnly fallback.

- [ ] **Step 3: Phase B 종료 commit**

```bash
git commit --allow-empty -m "chore(phase-b): 전체화면+얼굴 PIP 캡쳐 완료"
```

---

## Phase C — 룸 히스토리 피드

### Task C1: Supabase 마이그레이션 — 30일 TTL + hidden_for_receiver + 새 RPC

**Files:**
- Create: `supabase/migrations/20260521000200_v0_2_history.sql`

- [ ] **Step 1: Write migration**

Create `supabase/migrations/20260521000200_v0_2_history.sql`:

```sql
-- Phase C: 30-day retention + history RPCs + hidden_for_receiver flag

alter table public.messages
    add column if not exists hidden_for_receiver boolean not null default false;

-- Update default expires_at trigger / setter (current code sets expires_at on insert)
-- We change app code to set 30d instead of 24h; here we patch cleanup function.

create or replace function public.ping_cleanup_expired_data()
returns void
language plpgsql
security definer
set search_path = public
as $$
declare
    msg_record record;
begin
    -- Delete messages older than 30 days (regardless of expires_at)
    for msg_record in
        select m.id, m.video_url, m.sender_uid
        from public.messages m
        where m.created_at < now() - interval '30 days'
    loop
        delete from storage.objects
        where bucket_id = 'ping-videos'
          and name = msg_record.video_url;
        delete from public.messages where id = msg_record.id;
    end loop;

    -- Invitations / links: keep 7-day rule
    delete from public.invitations where expires_at < now();
    delete from public.invite_links where expires_at < now();
end;
$$;

-- Room messages with pagination
create or replace function public.ping_room_messages(
    room_uuid uuid,
    before_ts timestamptz default null,
    page_limit int default 50
) returns setof public.messages
language plpgsql
stable
security definer
set search_path = public
as $$
declare
    me uuid := auth.uid();
begin
    if me is null then
        raise exception 'auth required';
    end if;

    if not exists (
        select 1 from public.room_members where room_id = room_uuid and uid = me
    ) then
        raise exception 'not a member';
    end if;

    return query
    select * from public.messages
    where room_id = room_uuid
      and (receiver_uid = me or sender_uid = me)
      and (before_ts is null or created_at < before_ts)
      and (hidden_for_receiver = false or sender_uid = me)
    order by created_at desc
    limit page_limit;
end;
$$;

create or replace function public.ping_delete_message(message_uuid uuid)
returns void
language plpgsql
security definer
set search_path = public
as $$
declare
    me uuid := auth.uid();
    msg record;
begin
    select sender_uid, video_url into msg from public.messages where id = message_uuid;
    if msg is null then return; end if;
    if msg.sender_uid != me then
        raise exception 'only sender can delete';
    end if;

    delete from storage.objects where bucket_id = 'ping-videos' and name = msg.video_url;
    delete from public.messages where id = message_uuid;
end;
$$;

create or replace function public.ping_hide_message_for_receiver(message_uuid uuid)
returns void
language plpgsql
security definer
set search_path = public
as $$
declare
    me uuid := auth.uid();
begin
    update public.messages
       set hidden_for_receiver = true
     where id = message_uuid
       and receiver_uid = me;
end;
$$;

grant execute on function public.ping_room_messages(uuid, timestamptz, int) to authenticated;
grant execute on function public.ping_delete_message(uuid) to authenticated;
grant execute on function public.ping_hide_message_for_receiver(uuid) to authenticated;
```

- [ ] **Step 2: Update MessageService send to use 30-day expiresAt**

In `Ping/Backend/MessageService.swift`, change `expiresAt`:

```swift
        let expiresAt = Date().addingTimeInterval(30 * 24 * 60 * 60)
```

- [ ] **Step 3: Apply migration**

Run: `npx supabase db push`
Expected: success.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260521000200_v0_2_history.sql Ping/Backend/MessageService.swift
git commit -m "feat(db): 30-day retention + history RPCs + hidden_for_receiver"
```

---

### Task C2: HotkeyManager — Option+O for history toggle

**Files:**
- Modify: `Ping/Hotkey/HotkeyManager.swift`
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Add historyToggle shortcut name**

In `Ping/Hotkey/HotkeyManager.swift`:

```swift
extension KeyboardShortcuts.Name {
    static let pingTrigger = Self("pingTrigger", default: .init(.p, modifiers: [.option]))
    static let appearanceToggle = Self("appearanceToggle", default: .init(.d, modifiers: [.option, .shift]))
    static let historyToggle = Self("historyToggle", default: .init(.o, modifiers: [.option]))
}
```

Then in `HotkeyManager.register`, add a parameter and handler:

```swift
    func register(
        onPress: @escaping () -> Void,
        onAppearanceToggle: @escaping () -> Void,
        onHistoryToggle: @escaping () -> Void
    ) {
        KeyboardShortcuts.onKeyDown(for: .pingTrigger) {
            onPress()
        }
        KeyboardShortcuts.onKeyDown(for: .appearanceToggle) {
            onAppearanceToggle()
        }
        KeyboardShortcuts.onKeyDown(for: .historyToggle) {
            onHistoryToggle()
        }
    }
```

- [ ] **Step 2: Wire in AppDelegate**

In `Ping/AppDelegate.swift`, update `setupHotkey`:

```swift
    private func setupHotkey() {
        HotkeyManager.shared.register(
            onPress: { [weak self] in self?.toggleMirror() },
            onAppearanceToggle: { [weak self] in self?.toggleAppearanceMode() },
            onHistoryToggle: { [weak self] in self?.toggleHistory() }
        )
    }

    private var historyWindow: HistoryWindow?

    private func toggleHistory() {
        if let historyWindow {
            historyWindow.orderOut(nil)
            self.historyWindow = nil
            return
        }
        let window = HistoryWindow(
            appState: appState,
            messageService: messageService,
            cacheService: HistoryCacheService.shared,
            onClose: { [weak self] in
                self?.historyWindow?.orderOut(nil)
                self?.historyWindow = nil
            }
        )
        window.makeKeyAndOrderFront(nil)
        historyWindow = window
    }
```

- [ ] **Step 3: Add "히스토리 열기" to status menu**

In `Ping/StatusMenuBuilder.swift`, add a menu item:

```swift
        menu.addItem(command(title: "히스토리 열기", action: Selector(("toggleHistoryAction")), shortcutName: .historyToggle, target: target))
```

And in `Ping/AppDelegate.swift`, add the @objc selector:

```swift
    @objc func toggleHistoryAction() { toggleHistory() }
```

- [ ] **Step 4: Build (HistoryWindow + HistoryCacheService not yet defined, will fail)**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: FAIL — `HistoryWindow` / `HistoryCacheService` not found. Resolved in next tasks.

- [ ] **Step 5: Commit (WIP)**

```bash
git add Ping/Hotkey/HotkeyManager.swift Ping/AppDelegate.swift Ping/StatusMenuBuilder.swift
git commit -m "feat(hotkey): add Option+O history toggle (impl pending)"
```

---

### Task C3: MessageService.roomMessages + HistoryViewModel (TDD)

**Files:**
- Modify: `Ping/Backend/MessageService.swift`
- Create: `Ping/UI/History/HistoryViewModel.swift`
- Test: `PingTests/HistoryViewModelTests.swift`

- [ ] **Step 1: Add roomMessages RPC client**

In `Ping/Backend/MessageService.swift`:

```swift
    func roomMessages(roomId: String, beforeTimestamp: Date? = nil, limit: Int = 50) async throws -> [VideoMessage] {
        var body: [String: Any] = [
            "room_uuid": roomId,
            "page_limit": limit
        ]
        if let beforeTimestamp {
            body["before_ts"] = ISO8601DateFormatter.shared.string(from: beforeTimestamp)
        }
        return try await client.rpcArray("ping_room_messages", body: body)
    }

    func deleteMessage(messageId: String) async throws {
        try await client.rpcVoid("ping_delete_message", body: ["message_uuid": messageId])
    }

    func hideMessageForReceiver(messageId: String) async throws {
        try await client.rpcVoid("ping_hide_message_for_receiver", body: ["message_uuid": messageId])
    }
```

If `ISO8601DateFormatter.shared` is not defined, add to a utility file:

```swift
extension ISO8601DateFormatter {
    static let shared: ISO8601DateFormatter = {
        let f = ISO8601DateFormatter()
        f.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
        return f
    }()
}
```

- [ ] **Step 2: Write failing test for HistoryViewModel grouping**

Create `PingTests/HistoryViewModelTests.swift`:

```swift
import XCTest
@testable import Ping

@MainActor
final class HistoryViewModelTests: XCTestCase {
    func test_groupedByDay_buildsDateHeaders() {
        let cal = Calendar(identifier: .gregorian)
        let today = Date()
        let yesterday = cal.date(byAdding: .day, value: -1, to: today)!

        let msgs: [VideoMessage] = [
            makeMsg(id: "m1", createdAt: today),
            makeMsg(id: "m2", createdAt: today),
            makeMsg(id: "m3", createdAt: yesterday)
        ]

        let groups = HistoryViewModel.groupByDay(messages: msgs, calendar: cal)
        XCTAssertEqual(groups.count, 2)
        XCTAssertEqual(groups[0].messages.map(\.id), ["m1", "m2"])
        XCTAssertEqual(groups[1].messages.map(\.id), ["m3"])
    }

    private func makeMsg(id: String, createdAt: Date) -> VideoMessage {
        var m = VideoMessage(
            id: id,
            roomId: "r",
            senderUid: "u",
            receiverUid: "r",
            senderNickname: "nick",
            videoId: "v",
            videoUrl: "u/v.mp4",
            durationMs: 2000,
            mirrorPosition: MirrorPosition(xRatio: 0.5, yRatio: 0.5),
            status: .uploaded,
            createdAt: createdAt,
            expiresAt: createdAt.addingTimeInterval(60),
            captureMode: .faceOnly,
            aspectRatio: nil
        )
        return m
    }
}

extension VideoMessage {
    init(
        id: String?, roomId: String, senderUid: String, receiverUid: String,
        senderNickname: String, videoId: String, videoUrl: String, durationMs: Int,
        mirrorPosition: MirrorPosition, status: MessageStatus,
        createdAt: Date?, expiresAt: Date,
        captureMode: CaptureMode, aspectRatio: Double?
    ) {
        self.id = id
        self.roomId = roomId
        self.senderUid = senderUid
        self.receiverUid = receiverUid
        self.senderNickname = senderNickname
        self.videoId = videoId
        self.videoUrl = videoUrl
        self.durationMs = durationMs
        self.mirrorPosition = mirrorPosition
        self.status = status
        self.createdAt = createdAt
        self.expiresAt = expiresAt
        self.captureMode = captureMode
        self.aspectRatio = aspectRatio
    }
}
```

- [ ] **Step 3: Run test to verify it fails**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/HistoryViewModelTests`
Expected: FAIL — `HistoryViewModel` not defined.

- [ ] **Step 4: Implement HistoryViewModel**

Create `Ping/UI/History/HistoryViewModel.swift`:

```swift
import Foundation
import Combine

@MainActor
final class HistoryViewModel: ObservableObject {
    struct DayGroup: Identifiable {
        let date: Date
        let messages: [VideoMessage]
        var id: TimeInterval { date.timeIntervalSince1970 }
    }

    @Published var selectedRoomId: String?
    @Published var groups: [DayGroup] = []
    @Published var isLoading: Bool = false
    @Published var expandedMessageId: String?

    private let messageService: MessageService
    private var loadedMessages: [VideoMessage] = []

    init(messageService: MessageService) {
        self.messageService = messageService
    }

    func selectRoom(_ roomId: String) async {
        selectedRoomId = roomId
        loadedMessages = []
        groups = []
        await loadMore()
    }

    func loadMore() async {
        guard let roomId = selectedRoomId, !isLoading else { return }
        isLoading = true
        defer { isLoading = false }
        let before = loadedMessages.last?.createdAt
        do {
            let next = try await messageService.roomMessages(roomId: roomId, beforeTimestamp: before, limit: 50)
            loadedMessages.append(contentsOf: next)
            groups = Self.groupByDay(messages: loadedMessages, calendar: .current)
        } catch {
            NSLog("History load failed: \(error)")
        }
    }

    static func groupByDay(messages: [VideoMessage], calendar: Calendar) -> [DayGroup] {
        let sorted = messages.compactMap { msg -> (Date, VideoMessage)? in
            guard let created = msg.createdAt else { return nil }
            return (created, msg)
        }.sorted { $0.0 > $1.0 }

        var groups: [DayGroup] = []
        var currentDate: Date?
        var currentMsgs: [VideoMessage] = []

        for (date, msg) in sorted {
            let day = calendar.startOfDay(for: date)
            if day != currentDate {
                if let currentDate {
                    groups.append(DayGroup(date: currentDate, messages: currentMsgs))
                }
                currentDate = day
                currentMsgs = [msg]
            } else {
                currentMsgs.append(msg)
            }
        }
        if let currentDate {
            groups.append(DayGroup(date: currentDate, messages: currentMsgs))
        }
        return groups
    }
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test -only-testing:PingTests/HistoryViewModelTests`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add Ping/Backend/MessageService.swift Ping/UI/History/HistoryViewModel.swift PingTests/HistoryViewModelTests.swift
git commit -m "feat(history): MessageService.roomMessages + HistoryViewModel grouping"
```

---

### Task C4: HistoryCacheService — 영상 prefetch + LRU cache

**Files:**
- Create: `Ping/Backend/HistoryCacheService.swift`

- [ ] **Step 1: Implement cache service**

Create `Ping/Backend/HistoryCacheService.swift`:

```swift
import Foundation

@MainActor
final class HistoryCacheService {
    static let shared = HistoryCacheService()

    private let baseDir: URL
    private let maxBytes: Int64 = 500 * 1024 * 1024

    init() {
        let docs = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!
        baseDir = docs.appendingPathComponent("Ping/cache")
        try? FileManager.default.createDirectory(at: baseDir, withIntermediateDirectories: true)
    }

    func localURL(roomId: String, messageId: String) -> URL {
        let roomDir = baseDir.appendingPathComponent(roomId)
        try? FileManager.default.createDirectory(at: roomDir, withIntermediateDirectories: true)
        return roomDir.appendingPathComponent("\(messageId).mp4")
    }

    func cachedFile(roomId: String, messageId: String) -> URL? {
        let url = localURL(roomId: roomId, messageId: messageId)
        return FileManager.default.fileExists(atPath: url.path) ? url : nil
    }

    func storeDownload(roomId: String, messageId: String, sourceTemp: URL) throws -> URL {
        let dest = localURL(roomId: roomId, messageId: messageId)
        try? FileManager.default.removeItem(at: dest)
        try FileManager.default.moveItem(at: sourceTemp, to: dest)
        Task { await evictIfNeeded() }
        return dest
    }

    func evictIfNeeded() async {
        let files = (try? FileManager.default.subpathsOfDirectory(atPath: baseDir.path)) ?? []
        var attrs: [(URL, Date, Int64)] = []
        for sub in files {
            let url = baseDir.appendingPathComponent(sub)
            guard let a = try? FileManager.default.attributesOfItem(atPath: url.path),
                  let size = a[.size] as? Int64,
                  let mtime = a[.modificationDate] as? Date,
                  let type = a[.type] as? FileAttributeType,
                  type == .typeRegular else { continue }
            attrs.append((url, mtime, size))
        }

        let total = attrs.map(\.2).reduce(0, +)
        guard total > maxBytes else { return }

        // LRU: oldest mtime first
        let sorted = attrs.sorted { $0.1 < $1.1 }
        var remaining = total
        for (url, _, size) in sorted {
            if remaining <= maxBytes { break }
            try? FileManager.default.removeItem(at: url)
            remaining -= size
        }
    }

    func cleanupOlderThan(days: Int) async {
        let cutoff = Date().addingTimeInterval(-Double(days * 24 * 60 * 60))
        let files = (try? FileManager.default.subpathsOfDirectory(atPath: baseDir.path)) ?? []
        for sub in files {
            let url = baseDir.appendingPathComponent(sub)
            guard let a = try? FileManager.default.attributesOfItem(atPath: url.path),
                  let mtime = a[.modificationDate] as? Date else { continue }
            if mtime < cutoff {
                try? FileManager.default.removeItem(at: url)
            }
        }
    }
}
```

- [ ] **Step 2: Build to verify**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: BUILD SUCCEEDED (HistoryWindow still missing — next task).

- [ ] **Step 3: Commit**

```bash
git add Ping/Backend/HistoryCacheService.swift
git commit -m "feat(history): cache service with LRU eviction and 30d cleanup"
```

---

### Task C5: HistoryWindow + sidebar + timeline UI

**Files:**
- Create: `Ping/UI/History/HistoryWindow.swift`
- Create: `Ping/UI/History/HistoryView.swift`
- Create: `Ping/UI/History/HistorySidebar.swift`
- Create: `Ping/UI/History/RoomTimelineView.swift`
- Create: `Ping/UI/History/MessageRowView.swift`

- [ ] **Step 1: Create HistoryWindow shell**

Create `Ping/UI/History/HistoryWindow.swift`:

```swift
import AppKit
import SwiftUI

@MainActor
final class HistoryWindow: NSWindow {
    init(
        appState: AppState,
        messageService: MessageService,
        cacheService: HistoryCacheService,
        onClose: @escaping () -> Void
    ) {
        super.init(
            contentRect: NSRect(x: 0, y: 0, width: 900, height: 600),
            styleMask: [.titled, .closable, .miniaturizable, .resizable],
            backing: .buffered,
            defer: false
        )
        self.title = "Ping 히스토리"
        self.minSize = NSSize(width: 640, height: 480)
        self.isReleasedWhenClosed = false
        self.center()

        let viewModel = HistoryViewModel(messageService: messageService)
        let host = NSHostingView(rootView:
            HistoryView(appState: appState, viewModel: viewModel, cacheService: cacheService)
        )
        host.frame = contentView!.bounds
        host.autoresizingMask = [.width, .height]
        contentView?.addSubview(host)
    }
}
```

- [ ] **Step 2: Create HistoryView (sidebar + main)**

Create `Ping/UI/History/HistoryView.swift`:

```swift
import SwiftUI

struct HistoryView: View {
    @ObservedObject var appState: AppState
    @ObservedObject var viewModel: HistoryViewModel
    let cacheService: HistoryCacheService

    var body: some View {
        HSplitView {
            HistorySidebar(rooms: appState.rooms, selectedRoomId: $viewModel.selectedRoomId)
                .frame(minWidth: 220, idealWidth: 240, maxWidth: 320)
                .onChange(of: viewModel.selectedRoomId) { _, newId in
                    guard let id = newId else { return }
                    Task { await viewModel.selectRoom(id) }
                }

            if viewModel.selectedRoomId != nil {
                RoomTimelineView(viewModel: viewModel, cacheService: cacheService)
                    .frame(minWidth: 400)
            } else {
                VStack {
                    Text("좌측에서 룸을 선택하세요")
                        .foregroundStyle(.secondary)
                }
                .frame(maxWidth: .infinity, maxHeight: .infinity)
            }
        }
    }
}
```

- [ ] **Step 3: Create HistorySidebar**

Create `Ping/UI/History/HistorySidebar.swift`:

```swift
import SwiftUI

struct HistorySidebar: View {
    let rooms: [Room]
    @Binding var selectedRoomId: String?

    var body: some View {
        List(rooms, id: \.id, selection: $selectedRoomId) { room in
            VStack(alignment: .leading, spacing: 4) {
                Text(room.name)
                    .font(.headline)
                    .lineLimit(1)
                Text("\(room.memberUids.count)명")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            .tag(room.id)
        }
        .listStyle(.sidebar)
    }
}
```

- [ ] **Step 4: Create RoomTimelineView + MessageRowView**

Create `Ping/UI/History/MessageRowView.swift`:

```swift
import SwiftUI
import AVKit

struct MessageRowView: View {
    let message: VideoMessage
    let isMine: Bool
    let isExpanded: Bool
    let onTap: () -> Void
    let cacheService: HistoryCacheService

    var body: some View {
        HStack {
            if isMine { Spacer() }
            VStack(alignment: isMine ? .trailing : .leading, spacing: 4) {
                if isExpanded {
                    InlinePlayerView(message: message, cacheService: cacheService)
                        .frame(maxWidth: 360)
                } else {
                    thumbnail
                }
                metadata
            }
            if !isMine { Spacer() }
        }
        .padding(.vertical, 4)
        .contentShape(Rectangle())
        .onTapGesture { onTap() }
    }

    private var thumbnail: some View {
        Group {
            if message.captureMode == .faceOnly {
                Circle().fill(Color.gray.opacity(0.3)).frame(width: 60, height: 60)
            } else {
                RoundedRectangle(cornerRadius: 8).fill(Color.gray.opacity(0.3))
                    .aspectRatio(message.aspectRatio ?? 1.78, contentMode: .fit)
                    .frame(maxWidth: 90)
            }
        }
        .overlay(Image(systemName: "play.fill").foregroundStyle(.white))
    }

    private var metadata: some View {
        HStack(spacing: 6) {
            if message.captureMode == .screenFace {
                Image(systemName: "rectangle.fill").font(.caption2)
            } else {
                Image(systemName: "circle.fill").font(.caption2)
            }
            if let date = message.createdAt {
                Text(date.formatted(.dateTime.hour().minute()))
                    .font(.caption)
            }
        }
        .foregroundStyle(.secondary)
    }
}
```

Create `Ping/UI/History/RoomTimelineView.swift`:

```swift
import SwiftUI

struct RoomTimelineView: View {
    @ObservedObject var viewModel: HistoryViewModel
    let cacheService: HistoryCacheService
    @EnvironmentObject var appState: AppState

    var body: some View {
        ScrollView {
            LazyVStack(alignment: .leading, spacing: 12, pinnedViews: [.sectionHeaders]) {
                ForEach(viewModel.groups) { group in
                    Section(header: dayHeader(group.date)) {
                        ForEach(group.messages, id: \.id) { msg in
                            MessageRowView(
                                message: msg,
                                isMine: msg.senderUid == appState.currentUser?.id,
                                isExpanded: viewModel.expandedMessageId == msg.id,
                                onTap: {
                                    if viewModel.expandedMessageId == msg.id {
                                        viewModel.expandedMessageId = nil
                                    } else {
                                        viewModel.expandedMessageId = msg.id
                                    }
                                },
                                cacheService: cacheService
                            )
                        }
                    }
                }

                if viewModel.isLoading {
                    ProgressView().padding()
                }
            }
            .padding(.horizontal, 16)
        }
    }

    private func dayHeader(_ date: Date) -> some View {
        Text(dayLabel(date))
            .font(.caption.weight(.semibold))
            .foregroundStyle(.secondary)
            .padding(.vertical, 6)
            .frame(maxWidth: .infinity, alignment: .leading)
            .background(Color(NSColor.windowBackgroundColor))
    }

    private func dayLabel(_ date: Date) -> String {
        let cal = Calendar.current
        if cal.isDateInToday(date) { return "오늘" }
        if cal.isDateInYesterday(date) { return "어제" }
        return date.formatted(.dateTime.month().day().weekday(.abbreviated))
    }
}
```

- [ ] **Step 5: Build to verify (InlinePlayerView still missing)**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`
Expected: FAIL — `InlinePlayerView` not defined. Fixed in next task.

- [ ] **Step 6: Commit (WIP)**

```bash
git add Ping/UI/History/
git commit -m "feat(history): window shell + sidebar + timeline (player pending)"
```

---

### Task C6: InlinePlayerView — 인라인 자동재생 + Enter/Esc/Space

**Files:**
- Create: `Ping/UI/History/InlinePlayerView.swift`

- [ ] **Step 1: Implement InlinePlayerView**

Create `Ping/UI/History/InlinePlayerView.swift`:

```swift
import SwiftUI
import AVKit

struct InlinePlayerView: View {
    let message: VideoMessage
    let cacheService: HistoryCacheService

    @State private var localURL: URL?
    @State private var error: String?

    var body: some View {
        Group {
            if let localURL {
                PlayerBox(url: localURL, aspectRatio: message.aspectRatio ?? 1, isCircle: message.captureMode == .faceOnly)
            } else if let error {
                Text(error).foregroundStyle(.red).font(.caption)
            } else {
                ProgressView()
            }
        }
        .frame(maxWidth: message.captureMode == .faceOnly ? 180 : 360)
        .task { await load() }
    }

    private func load() async {
        guard let id = message.id else { return }
        if let cached = cacheService.cachedFile(roomId: message.roomId, messageId: id) {
            localURL = cached
            return
        }
        do {
            // Download via StorageService — share logic from AppDelegate.downloadMessageVideo if accessible
            let storage = StorageService()
            let tempURL = try await storage.downloadVideo(remotePath: message.videoUrl)
            localURL = try cacheService.storeDownload(roomId: message.roomId, messageId: id, sourceTemp: tempURL)
        } catch let err {
            error = err.localizedDescription
        }
    }

    struct PlayerBox: NSViewRepresentable {
        let url: URL
        let aspectRatio: Double
        let isCircle: Bool

        func makeNSView(context: Context) -> NSView {
            let container = NSView()
            container.wantsLayer = true
            let item = AVPlayerItem(url: url)
            let player = AVPlayer(playerItem: item)
            let layer = AVPlayerLayer(player: player)
            layer.videoGravity = .resizeAspectFill
            container.layer?.addSublayer(layer)
            player.play()
            context.coordinator.player = player
            return container
        }

        func updateNSView(_ nsView: NSView, context: Context) {
            if let layer = nsView.layer?.sublayers?.first as? AVPlayerLayer {
                layer.frame = nsView.bounds
                if isCircle {
                    let mask = CAShapeLayer()
                    let dim = min(nsView.bounds.width, nsView.bounds.height)
                    mask.path = CGPath(ellipseIn: CGRect(x: (nsView.bounds.width - dim) / 2, y: (nsView.bounds.height - dim) / 2, width: dim, height: dim), transform: nil)
                    nsView.layer?.mask = mask
                } else {
                    let mask = CAShapeLayer()
                    mask.path = CGPath(roundedRect: nsView.bounds, cornerWidth: 12, cornerHeight: 12, transform: nil)
                    nsView.layer?.mask = mask
                }
            }
        }

        func makeCoordinator() -> Coord { Coord() }

        final class Coord {
            var player: AVPlayer?
        }
    }
}
```

- [ ] **Step 2: Add downloadVideo to StorageService if missing**

Read `Ping/Backend/StorageService.swift`. If `downloadVideo(remotePath:)` is not exposed, add:

```swift
    func downloadVideo(remotePath: String) async throws -> URL {
        // Use existing private download helper from AppDelegate, or implement direct GET.
        let url = try await client.signedDownloadURL(bucket: "ping-videos", path: remotePath, expiresIn: 600)
        let (data, _) = try await URLSession.shared.data(from: url)
        let tempURL = FileManager.default.temporaryDirectory.appendingPathComponent("ping-dl-\(UUID().uuidString).mp4")
        try data.write(to: tempURL)
        return tempURL
    }
```

(If `client.signedDownloadURL` doesn't exist, port the logic from AppDelegate's existing download path. Read AppDelegate's `downloadMessageVideo` for reference.)

- [ ] **Step 3: Build and smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

Option+O → 히스토리 윈도우 → 룸 선택 → 메시지 row 클릭 → 인라인 자동 재생.

- [ ] **Step 4: Commit**

```bash
git add Ping/UI/History/InlinePlayerView.swift Ping/Backend/StorageService.swift
git commit -m "feat(history): inline auto-play with cached download"
```

---

### Task C7: 히스토리 단축키 (↑↓/Enter/Esc/Space/Cmd+W)

**Files:**
- Modify: `Ping/UI/History/RoomTimelineView.swift`
- Modify: `Ping/UI/History/HistoryView.swift`

- [ ] **Step 1: Add key navigation via local event monitor**

In `Ping/UI/History/HistoryView.swift`, add:

```swift
    @State private var keyMonitor: Any?

    var body: some View {
        // existing HSplitView ...
        .onAppear {
            keyMonitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { event in
                handleKey(event) ? nil : event
            }
        }
        .onDisappear {
            if let keyMonitor {
                NSEvent.removeMonitor(keyMonitor)
                self.keyMonitor = nil
            }
        }
    }

    private func handleKey(_ event: NSEvent) -> Bool {
        guard let group = viewModel.groups.first else { return false }
        let allMsgs = viewModel.groups.flatMap(\.messages)
        let current = viewModel.expandedMessageId.flatMap { id in
            allMsgs.firstIndex(where: { $0.id == id })
        }

        switch event.keyCode {
        case 126: // up
            if let i = current, i > 0 {
                viewModel.expandedMessageId = allMsgs[i - 1].id
            } else if current == nil, let first = allMsgs.first {
                viewModel.expandedMessageId = first.id
            }
            return true
        case 125: // down
            if let i = current, i + 1 < allMsgs.count {
                viewModel.expandedMessageId = allMsgs[i + 1].id
            } else if current == nil, let first = allMsgs.first {
                viewModel.expandedMessageId = first.id
            }
            return true
        case 53: // esc
            if viewModel.expandedMessageId != nil {
                viewModel.expandedMessageId = nil
            } else {
                NSApp.keyWindow?.close()
            }
            return true
        default:
            return false
        }
    }
```

- [ ] **Step 2: Build and smoke**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

히스토리 열고 ↓ ↑ 으로 메시지 이동, Esc로 collapse 또는 윈도우 닫기.

- [ ] **Step 3: Commit**

```bash
git add Ping/UI/History/HistoryView.swift
git commit -m "feat(history): keyboard navigation up/down/esc"
```

---

### Task C8: 인라인 재생 paused/replay/expand + 메시지 액션 (호버 저장·삭제)

**Files:**
- Modify: `Ping/UI/History/InlinePlayerView.swift`
- Modify: `Ping/UI/History/HistoryView.swift`
- Modify: `Ping/UI/History/MessageRowView.swift`
- Modify: `Ping/UI/History/HistoryViewModel.swift`

- [ ] **Step 1: Add paused-on-end + replay + expand support to InlinePlayerView**

Replace `Ping/UI/History/InlinePlayerView.swift` entirely:

```swift
import SwiftUI
import AVKit

final class InlinePlayerController: ObservableObject {
    @Published var isPaused: Bool = false
    weak var player: AVPlayer?

    func replay() {
        player?.seek(to: .zero)
        player?.play()
        isPaused = false
    }
}

struct InlinePlayerView: View {
    let message: VideoMessage
    let cacheService: HistoryCacheService
    @ObservedObject var controller: InlinePlayerController
    let onExpand: (AVPlayer) -> Void

    @State private var localURL: URL?
    @State private var error: String?

    var body: some View {
        Group {
            if let localURL {
                PlayerBox(
                    url: localURL,
                    aspectRatio: message.aspectRatio ?? 1,
                    isCircle: message.captureMode == .faceOnly,
                    controller: controller
                )
            } else if let error {
                Text(error).foregroundStyle(.red).font(.caption)
            } else {
                ProgressView()
            }
        }
        .frame(maxWidth: message.captureMode == .faceOnly ? 180 : 360)
        .task { await load() }
    }

    private func load() async {
        guard let id = message.id else { return }
        if let cached = cacheService.cachedFile(roomId: message.roomId, messageId: id) {
            localURL = cached
            return
        }
        do {
            let storage = StorageService()
            let tempURL = try await storage.downloadVideo(remotePath: message.videoUrl)
            localURL = try cacheService.storeDownload(roomId: message.roomId, messageId: id, sourceTemp: tempURL)
        } catch let err {
            error = err.localizedDescription
        }
    }

    struct PlayerBox: NSViewRepresentable {
        let url: URL
        let aspectRatio: Double
        let isCircle: Bool
        @ObservedObject var controller: InlinePlayerController

        func makeNSView(context: Context) -> NSView {
            let container = NSView()
            container.wantsLayer = true
            let item = AVPlayerItem(url: url)
            let player = AVPlayer(playerItem: item)
            let layer = AVPlayerLayer(player: player)
            layer.videoGravity = .resizeAspectFill
            container.layer?.addSublayer(layer)
            controller.player = player
            context.coordinator.observer = NotificationCenter.default.addObserver(
                forName: .AVPlayerItemDidPlayToEndTime,
                object: item,
                queue: .main
            ) { [weak player] _ in
                player?.pause()
                Task { @MainActor in controller.isPaused = true }
            }
            player.play()
            return container
        }

        func updateNSView(_ nsView: NSView, context: Context) {
            if let layer = nsView.layer?.sublayers?.first as? AVPlayerLayer {
                layer.frame = nsView.bounds
                let mask = CAShapeLayer()
                if isCircle {
                    let dim = min(nsView.bounds.width, nsView.bounds.height)
                    mask.path = CGPath(ellipseIn: CGRect(x: (nsView.bounds.width - dim) / 2, y: (nsView.bounds.height - dim) / 2, width: dim, height: dim), transform: nil)
                } else {
                    mask.path = CGPath(roundedRect: nsView.bounds, cornerWidth: 12, cornerHeight: 12, transform: nil)
                }
                nsView.layer?.mask = mask
            }
        }

        func makeCoordinator() -> Coord { Coord() }

        final class Coord {
            var observer: Any?
            deinit { if let o = observer { NotificationCenter.default.removeObserver(o) } }
        }
    }
}
```

- [ ] **Step 2: Wire controller through MessageRowView**

In `Ping/UI/History/MessageRowView.swift`, add a controller param:

```swift
struct MessageRowView: View {
    let message: VideoMessage
    let isMine: Bool
    let isExpanded: Bool
    let onTap: () -> Void
    let cacheService: HistoryCacheService
    @ObservedObject var inlineController: InlinePlayerController
    let onExpand: (AVPlayer) -> Void
    let onSave: () -> Void
    let onDelete: () -> Void

    @State private var isHovered: Bool = false

    var body: some View {
        HStack {
            if isMine { Spacer() }
            VStack(alignment: isMine ? .trailing : .leading, spacing: 4) {
                if isExpanded {
                    InlinePlayerView(message: message, cacheService: cacheService, controller: inlineController, onExpand: onExpand)
                } else {
                    thumbnail
                }
                metadata
            }
            if isHovered && !isExpanded {
                HStack(spacing: 8) {
                    Button(action: onSave) {
                        Image(systemName: "arrow.down.circle").imageScale(.small)
                    }.buttonStyle(.plain)
                    Button(action: onDelete) {
                        Image(systemName: "trash").imageScale(.small)
                    }.buttonStyle(.plain)
                }
                .transition(.opacity)
            }
            if !isMine { Spacer() }
        }
        .padding(.vertical, 4)
        .contentShape(Rectangle())
        .onTapGesture { onTap() }
        .onHover { hovering in
            withAnimation(.easeInOut(duration: 0.15)) { isHovered = hovering }
        }
    }

    private var thumbnail: some View {
        Group {
            if message.captureMode == .faceOnly {
                Circle().fill(Color.gray.opacity(0.3)).frame(width: 60, height: 60)
            } else {
                RoundedRectangle(cornerRadius: 8).fill(Color.gray.opacity(0.3))
                    .aspectRatio(message.aspectRatio ?? 1.78, contentMode: .fit)
                    .frame(maxWidth: 90)
            }
        }
        .overlay(Image(systemName: "play.fill").foregroundStyle(.white))
    }

    private var metadata: some View {
        HStack(spacing: 6) {
            if message.captureMode == .screenFace {
                Image(systemName: "rectangle.fill").font(.caption2)
            } else {
                Image(systemName: "circle.fill").font(.caption2)
            }
            if let date = message.createdAt {
                Text(date.formatted(.dateTime.hour().minute()))
                    .font(.caption)
            }
        }
        .foregroundStyle(.secondary)
    }
}
```

- [ ] **Step 3: Wire actions and Enter/Space keys in HistoryView**

In `Ping/UI/History/HistoryView.swift`, add controller + actions and extend `handleKey`:

```swift
    @StateObject private var inlineController = InlinePlayerController()
    @State private var expandedPlaybackWindow: ExpandedPlaybackWindow?

    private func handleKey(_ event: NSEvent) -> Bool {
        let allMsgs = viewModel.groups.flatMap(\.messages)
        let current = viewModel.expandedMessageId.flatMap { id in
            allMsgs.firstIndex(where: { $0.id == id })
        }

        switch event.keyCode {
        case 126: // up
            if let i = current, i > 0 {
                viewModel.expandedMessageId = allMsgs[i - 1].id
            } else if current == nil, let first = allMsgs.first {
                viewModel.expandedMessageId = first.id
            }
            return true
        case 125: // down
            if let i = current, i + 1 < allMsgs.count {
                viewModel.expandedMessageId = allMsgs[i + 1].id
            } else if current == nil, let first = allMsgs.first {
                viewModel.expandedMessageId = first.id
            }
            return true
        case 36: // Enter — replay
            inlineController.replay()
            return true
        case 49: // Space — expand
            if let player = inlineController.player,
               let id = viewModel.expandedMessageId,
               let msg = allMsgs.first(where: { $0.id == id }),
               let screen = NSApp.keyWindow?.screen {
                let expanded = ExpandedPlaybackWindow(
                    player: player,
                    aspectRatio: msg.aspectRatio ?? 1,
                    on: screen,
                    onDismiss: { [weak expandedPlaybackWindow] in
                        expandedPlaybackWindow?.orderOut(nil)
                    }
                )
                expandedPlaybackWindow = expanded
                expanded.present()
            }
            return true
        case 53: // esc
            if viewModel.expandedMessageId != nil {
                viewModel.expandedMessageId = nil
            } else {
                NSApp.keyWindow?.close()
            }
            return true
        default:
            return false
        }
    }
```

And pass controller + actions through to `MessageRowView` in `RoomTimelineView`:

In `Ping/UI/History/RoomTimelineView.swift`, update the ForEach body:

```swift
            ForEach(group.messages, id: \.id) { msg in
                MessageRowView(
                    message: msg,
                    isMine: msg.senderUid == appState.currentUser?.id,
                    isExpanded: viewModel.expandedMessageId == msg.id,
                    onTap: {
                        if viewModel.expandedMessageId == msg.id {
                            viewModel.expandedMessageId = nil
                        } else {
                            viewModel.expandedMessageId = msg.id
                        }
                    },
                    cacheService: cacheService,
                    inlineController: viewModel.inlineController,
                    onExpand: { _ in /* handled via Space key in HistoryView */ },
                    onSave: { Task { await viewModel.save(message: msg, cacheService: cacheService) } },
                    onDelete: { Task { await viewModel.delete(message: msg) } }
                )
            }
```

Update `RoomTimelineView` to receive `cacheService` and accept these without complaint, and remove the `@EnvironmentObject` line — pass `appState` explicitly:

```swift
struct RoomTimelineView: View {
    @ObservedObject var viewModel: HistoryViewModel
    let cacheService: HistoryCacheService
    @ObservedObject var appState: AppState
    // ...
}
```

Update `HistoryView` to forward `appState`:

```swift
            RoomTimelineView(viewModel: viewModel, cacheService: cacheService, appState: appState)
```

- [ ] **Step 4: Add save/delete + inlineController to HistoryViewModel**

In `Ping/UI/History/HistoryViewModel.swift`, append:

```swift
    let inlineController = InlinePlayerController()

    func save(message: VideoMessage, cacheService: HistoryCacheService) async {
        guard let id = message.id else { return }
        if let cached = cacheService.cachedFile(roomId: message.roomId, messageId: id) {
            let isMine = false // unused unless we differentiate folder
            if isMine {
                try? LocalArchive.saveSent(from: cached, partnerName: message.senderNickname)
            } else {
                try? LocalArchive.saveReceived(from: cached, partnerName: message.senderNickname)
            }
        }
    }

    func delete(message: VideoMessage) async {
        guard let id = message.id else { return }
        do {
            // Determine if current user is sender
            // (HistoryViewModel doesn't know currentUser; pass it via init or AppState.shared)
            try await messageService.deleteMessage(messageId: id)
            loadedMessages.removeAll { $0.id == id }
            groups = Self.groupByDay(messages: loadedMessages, calendar: .current)
        } catch {
            // fallback: hide for receiver
            try? await messageService.hideMessageForReceiver(messageId: id)
            loadedMessages.removeAll { $0.id == id }
            groups = Self.groupByDay(messages: loadedMessages, calendar: .current)
        }
    }
```

(`LocalArchive.saveSent` / `saveReceived` are existing API — verify signatures in `Ping/Capture/LocalArchive.swift`.)

- [ ] **Step 5: Build and smoke test**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

히스토리 열기 → 메시지 클릭 → 인라인 재생 → 끝나면 paused → Enter로 다시 재생 → Space로 확대 모달 → 확대 모달에서 Esc → 다시 인라인 → Esc → collapse → 호버 시 저장/삭제 버튼 표시.

- [ ] **Step 6: Commit**

```bash
git add Ping/UI/History/
git commit -m "feat(history): paused/replay/expand + hover save/delete actions"
```

---

### Task C9: Phase C 회귀 검증

- [ ] **Step 1: Full test**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`

- [ ] **Step 2: End-to-end smoke**

- 두 계정에서 메시지 주고받기 → 양쪽 모두 히스토리 윈도우 열어 확인.
- Option+O 토글, 룸 선택, 메시지 인라인 재생, ↑↓ 이동, Esc collapse.

- [ ] **Step 3: Phase C 종료 commit**

```bash
git commit --allow-empty -m "chore(phase-c): 룸 히스토리 피드 완료"
```

---

## Phase D2 — 메뉴바 응답성 개선

### Task D2.1: 측정 — Instruments + signpost

**Files:**
- Modify: `Ping/AppDelegate.swift`
- Modify: `Ping/Backend/MessageService.swift`
- Modify: `Ping/Backend/RoomService.swift`
- Modify: `Ping/Backend/InvitationService.swift`

- [ ] **Step 1: Add os_signpost instrumentation**

In each polling service (`Ping/Backend/MessageService.swift` `observeIncoming`, `Ping/Backend/RoomService.swift` `observeMyRooms`, `Ping/Backend/InvitationService.swift` `observeIncoming`), wrap the polling cycle with signposts.

In `Ping/Backend/RoomService.swift`:

```swift
import OSLog

private let pollLog = Logger(subsystem: "com.youngminpark.ping.Ping", category: "polling")
private let signposter = OSSignposter(subsystem: "com.youngminpark.ping.Ping", category: "polling")

// Inside observeMyRooms's polling loop, before fetch:
let intervalId = signposter.makeSignpostID()
let interval = signposter.beginInterval("rooms-poll-cycle", id: intervalId)
// ... do fetch and decode ...
signposter.endInterval("rooms-poll-cycle", interval)
```

Do the same for InvitationService and MessageService observers.

In `Ping/StatusMenuBuilder.swift` or AppDelegate, instrument menu open via NSMenuDelegate:

In `Ping/AppDelegate.swift`, conform to `NSMenuDelegate`:

```swift
extension AppDelegate: NSMenuDelegate {
    nonisolated func menuWillOpen(_ menu: NSMenu) {
        let id = signposter.makeSignpostID()
        signposter.emitEvent("menu-will-open", id: id)
    }
}
```

And in `setupStatusBar`:

```swift
        item.menu?.delegate = self
```

- [ ] **Step 2: Build a Release-style profile build**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Release -destination "platform=macOS" build
```

- [ ] **Step 3: Profile with Instruments**

Manual: `Xcode → Open Developer Tool → Instruments → Time Profiler + os_signpost`. Attach to Ping. 메뉴 10회 open/close.

Analyze:
- Main thread sample density during `menuWillOpen` events.
- Overlap with `rooms-poll-cycle` / etc. signposts.
- Identify whether polling cycles block menu open.

Record findings (signpost delta, main-thread sample distribution) in a temporary `docs/superpowers/notes/2026-05-XX-menu-perf.md` file.

- [ ] **Step 4: Commit instrumentation**

```bash
git add Ping/Backend/RoomService.swift Ping/Backend/InvitationService.swift Ping/Backend/MessageService.swift Ping/AppDelegate.swift docs/superpowers/notes/
git commit -m "perf(menu): instrumentation with os_signpost for polling and menu open"
```

---

### Task D2.2: Polling을 메인 액터에서 분리

(Apply this if Task D2.1 confirms hypothesis 1.)

**Files:**
- Modify: `Ping/Backend/RoomService.swift`
- Modify: `Ping/Backend/InvitationService.swift`
- Modify: `Ping/Backend/MessageService.swift`

- [ ] **Step 1: Move polling task to non-main actor**

Example for `Ping/Backend/RoomService.swift`:

```swift
    func observeMyRooms(uid: String) -> AsyncStream<[Room]> {
        AsyncStream { continuation in
            let task = Task.detached(priority: .utility) {
                while !Task.isCancelled {
                    let result: Result<[Room], Error>
                    do {
                        let rooms: [Room] = try await SupabaseClient.shared.rpcArray("ping_my_rooms")
                        result = .success(rooms)
                    } catch {
                        result = .failure(error)
                    }

                    switch result {
                    case .success(let rooms):
                        continuation.yield(rooms)
                    case .failure(let error):
                        NSLog("Rooms polling failed: \(error)")
                    }

                    try? await Task.sleep(nanoseconds: 2_000_000_000)
                }
                continuation.finish()
            }
            continuation.onTermination = { _ in task.cancel() }
        }
    }
```

(Note: this requires `SupabaseClient.shared.rpcArray` to be Sendable / nonisolated. If it's `@MainActor`, mark it `nonisolated` for read-only RPC paths, or extract a `nonisolated let publicClient = ...` that wraps URLSession directly.)

Apply analogous changes to `InvitationService.observeIncoming` and `MessageService.observeIncoming`.

- [ ] **Step 2: AppDelegate consumes async stream and hops to main once**

In `Ping/AppDelegate.swift`, the consumer is already `Task { @MainActor in for await ... }` — no change needed if SwiftConcurrency hops automatically. Verify the for-await body runs on @MainActor.

- [ ] **Step 3: Re-profile with Instruments**

Repeat Task D2.1 step 3. Compare main thread sample distribution before/after. Expect main thread occupancy during poll cycles to drop significantly.

- [ ] **Step 4: Document delta**

Append measured before/after numbers to the perf note file.

- [ ] **Step 5: Build and run smoke**

```bash
xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build
```

기능 무결성 확인 (룸 polling, invitation polling, message polling 다 정상).

- [ ] **Step 6: Commit**

```bash
git add Ping/Backend/RoomService.swift Ping/Backend/InvitationService.swift Ping/Backend/MessageService.swift docs/superpowers/notes/
git commit -m "perf(polling): move polling to detached utility tasks off main actor"
```

---

### Task D2.3: 잔여 지연 있을 시 — menuWillOpen에서 polling suspend (조건부)

Apply only if D2.2 alone is insufficient.

**Files:**
- Modify: `Ping/AppDelegate.swift`

- [ ] **Step 1: Add menu open/close hooks**

```swift
extension AppDelegate: NSMenuDelegate {
    nonisolated func menuWillOpen(_ menu: NSMenu) {
        Task { @MainActor in
            roomObserverTask?.cancel()
            invitationObserverTask?.cancel()
            incomingMessageTask?.cancel()
            roomObserverTask = nil
            invitationObserverTask = nil
            incomingMessageTask = nil
        }
    }

    nonisolated func menuDidClose(_ menu: NSMenu) {
        Task { @MainActor in
            guard let uid = appState.currentUser?.id else { return }
            startObservers(uid: uid, opensRoomManagerWhenEmpty: false)
        }
    }
}
```

- [ ] **Step 2: Smoke test**

메뉴 열고 닫기 반복 — polling이 일시정지/재시작되며 정상 동작.

- [ ] **Step 3: Commit**

```bash
git add Ping/AppDelegate.swift
git commit -m "perf(menu): suspend polling while status menu is open"
```

---

### Task D2.4: Phase D2 종료 + 최종 회귀

- [ ] **Step 1: Full test**

Run: `xcodebuild -project Ping.xcodeproj -scheme Ping -destination "platform=macOS" test`

- [ ] **Step 2: Final smoke matrix**

- Phase A: 다시 찍기/재생 정상
- Phase B: 화면+얼굴 캡쳐 + Space 확대 + F 토글
- Phase C: Option+O 히스토리 + 인라인 재생 + 키 네비
- Phase D: 메뉴 응답 빨라짐 (Instruments delta)

- [ ] **Step 3: Bump version**

In `project.yml`, set `MARKETING_VERSION: 0.2.0`.

Run: `xcodegen generate && xcodebuild -project Ping.xcodeproj -scheme Ping -configuration Debug -destination "platform=macOS" build`

- [ ] **Step 4: Update CHANGELOG**

(Read `CHANGELOG.md`; if it exists, prepend a v0.2.0 section summarizing the four phases. If not, skip.)

- [ ] **Step 5: Final commit**

```bash
git add project.yml CHANGELOG.md
git commit -m "release: 0.2.0"
git tag v0.2.0
```

---

## 완료 정의 (Definition of Done)

전 phase 완료 시:
- `xcodebuild test`가 통과한다.
- 4개 phase의 수동 smoke 매트릭스를 통과한다.
- Supabase 마이그레이션 2개가 원격에 적용되어 있다.
- spec 문서의 모든 요구사항이 1개 이상의 task에 대응한다.
- v0.2.0 태그가 push 가능한 상태다.
